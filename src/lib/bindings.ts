// @ts-nocheck
// Auto-generated by tauri-specta. DO NOT EDIT.


// This file has been generated by Tauri Specta. Do not edit this file manually.

import { invoke as __TAURI_INVOKE } from "@tauri-apps/api/core";

/** Commands */
export const commands = {
	/**  Simple greeting command for demonstration purposes. */
	greet: (name: string) => typedError<string, string>(__TAURI_INVOKE("greet", { name })),
	/**
	 *  Loads user preferences from disk.
	 *  Returns default preferences if the file doesn't exist.
	 */
	loadPreferences: () => typedError<AppPreferences_Serialize, string>(__TAURI_INVOKE("load_preferences")),
	/**
	 *  Saves user preferences to disk.
	 *  Uses atomic write (temp file + rename) to prevent corruption.
	 */
	savePreferences: (preferences: AppPreferences) => typedError<null, string>(__TAURI_INVOKE("save_preferences", { preferences })),
	/**  Gets the current application version from Cargo.toml. */
	getAppVersion: () => __TAURI_INVOKE<string>("get_app_version"),
	/**
	 *  Sends a native system notification.
	 *  On mobile platforms, returns an error as notifications are not yet supported.
	 */
	sendNativeNotification: (title: string, body: string | null) => typedError<null, string>(__TAURI_INVOKE("send_native_notification", { title, body })),
	/**
	 *  Saves emergency data to a JSON file for later recovery.
	 *  Validates filename and enforces a 10MB size limit.
	 */
	saveEmergencyData: (filename: string, data: Value) => typedError<null, RecoveryError>(__TAURI_INVOKE("save_emergency_data", { filename, data })),
	/**
	 *  Loads emergency data from a previously saved JSON file.
	 *  Returns FileNotFound if the file doesn't exist.
	 */
	loadEmergencyData: (filename: string) => typedError<Value, RecoveryError>(__TAURI_INVOKE("load_emergency_data", { filename })),
	/**
	 *  Removes recovery files older than 7 days.
	 *  Returns the count of removed files.
	 */
	cleanupOldRecoveryFiles: () => typedError<number, RecoveryError>(__TAURI_INVOKE("cleanup_old_recovery_files")),
	/**  Gets the path to the Factory config file */
	getConfigPath: () => typedError<string, string>(__TAURI_INVOKE("get_config_path")),
	/**  Resets the config file to an empty JSON object */
	resetConfigFile: () => typedError<null, string>(__TAURI_INVOKE("reset_config_file")),
	/**  Loads custom models from settings.json */
	loadCustomModels: () => typedError<CustomModel_Serialize[], string>(__TAURI_INVOKE("load_custom_models")),
	/**  Saves custom models to settings.json (preserves other fields) */
	saveCustomModels: (models: CustomModel[]) => typedError<null, string>(__TAURI_INVOKE("save_custom_models", { models })),
	/**  Loads modelFavorites from settings.json. */
	getModelFavorites: () => typedError<string[], string>(__TAURI_INVOKE("get_model_favorites")),
	/**  Saves modelFavorites to settings.json (preserves other fields). */
	saveModelFavorites: (favorites: string[]) => typedError<null, string>(__TAURI_INVOKE("save_model_favorites", { favorites })),
	/**  Checks if legacy config.json exists and settings.json has customModels */
	checkLegacyConfig: () => typedError<boolean, string>(__TAURI_INVOKE("check_legacy_config")),
	/**  Deletes the legacy config.json file */
	deleteLegacyConfig: () => typedError<null, string>(__TAURI_INVOKE("delete_legacy_config")),
	/**  Fetches available models from a provider API */
	fetchModels: (provider: Provider, baseUrl: string, apiKey: string) => typedError<ModelInfo[], string>(__TAURI_INVOKE("fetch_models", { provider, baseUrl, apiKey })),
	/**  Gets the default model ID from sessionDefaultSettings.model */
	getDefaultModel: () => typedError<string | null, string>(__TAURI_INVOKE("get_default_model")),
	/**  Saves the default model ID to sessionDefaultSettings.model */
	saveDefaultModel: (modelId: string) => typedError<null, string>(__TAURI_INVOKE("save_default_model", { modelId })),
	/**
	 *  Gets the cloudSessionSync setting from settings.json
	 *  Returns true by default if not set
	 */
	getCloudSessionSync: () => typedError<boolean, string>(__TAURI_INVOKE("get_cloud_session_sync")),
	/**  Saves the cloudSessionSync setting to settings.json */
	saveCloudSessionSync: (enabled: boolean) => typedError<null, string>(__TAURI_INVOKE("save_cloud_session_sync", { enabled })),
	/**
	 *  Gets the reasoningEffort setting from settings.json
	 *  Returns None if not set (model-dependent default)
	 */
	getReasoningEffort: () => typedError<string | null, string>(__TAURI_INVOKE("get_reasoning_effort")),
	/**  Saves the reasoningEffort setting to settings.json */
	saveReasoningEffort: (value: string) => typedError<null, string>(__TAURI_INVOKE("save_reasoning_effort", { value })),
	/**
	 *  Gets the diffMode setting from settings.json
	 *  Returns "github" by default if not set
	 */
	getDiffMode: () => typedError<string, string>(__TAURI_INVOKE("get_diff_mode")),
	/**  Saves the diffMode setting to settings.json */
	saveDiffMode: (value: string) => typedError<null, string>(__TAURI_INVOKE("save_diff_mode", { value })),
	/**
	 *  Gets the todoDisplayMode setting from settings.json
	 *  Returns "pinned" by default if not set
	 */
	getTodoDisplayMode: () => typedError<string, string>(__TAURI_INVOKE("get_todo_display_mode")),
	/**  Saves the todoDisplayMode setting to settings.json */
	saveTodoDisplayMode: (value: string) => typedError<null, string>(__TAURI_INVOKE("save_todo_display_mode", { value })),
	/**
	 *  Gets the includeCoAuthoredByDroid setting from settings.json
	 *  Returns true by default if not set
	 */
	getIncludeCoAuthoredByDroid: () => typedError<boolean, string>(__TAURI_INVOKE("get_include_co_authored_by_droid")),
	/**  Saves the includeCoAuthoredByDroid setting to settings.json */
	saveIncludeCoAuthoredByDroid: (enabled: boolean) => typedError<null, string>(__TAURI_INVOKE("save_include_co_authored_by_droid", { enabled })),
	/**
	 *  Gets the showThinkingInMainView setting from settings.json
	 *  Returns false by default if not set
	 */
	getShowThinkingInMainView: () => typedError<boolean, string>(__TAURI_INVOKE("get_show_thinking_in_main_view")),
	/**  Saves the showThinkingInMainView setting to settings.json */
	saveShowThinkingInMainView: (enabled: boolean) => typedError<null, string>(__TAURI_INVOKE("save_show_thinking_in_main_view", { enabled })),
	/**
	 *  Gets the missionModelSettings from settings.json
	 *  Returns empty settings if not set
	 */
	getMissionModelSettings: () => typedError<MissionModelSettings_Serialize, string>(__TAURI_INVOKE("get_mission_model_settings")),
	/**  Saves the missionModelSettings to settings.json */
	saveMissionModelSettings: (settings: MissionModelSettings) => typedError<null, string>(__TAURI_INVOKE("save_mission_model_settings", { settings })),
	/**
	 *  Gets the sessionDefaultSettings from settings.json
	 *  Returns empty settings if not set
	 */
	getSessionDefaultSettings: () => typedError<SessionDefaultSettings_Serialize, string>(__TAURI_INVOKE("get_session_default_settings")),
	/**  Saves the sessionDefaultSettings to settings.json */
	saveSessionDefaultSettings: (settings: SessionDefaultSettings) => typedError<null, string>(__TAURI_INVOKE("save_session_default_settings", { settings })),
	/**
	 *  Gets the compactionModelMode setting from settings.json
	 *  Returns "current-model" by default if not set
	 */
	getCompactionModelMode: () => typedError<string, string>(__TAURI_INVOKE("get_compaction_model_mode")),
	/**  Saves the compactionModelMode setting to settings.json */
	saveCompactionModelMode: (value: string) => typedError<null, string>(__TAURI_INVOKE("save_compaction_model_mode", { value })),
	/**
	 *  Gets the compactionTokenLimit setting from settings.json
	 *  Returns 200000 by default if not set
	 */
	getCompactionTokenLimit: () => typedError<number, string>(__TAURI_INVOKE("get_compaction_token_limit")),
	/**  Saves the compactionTokenLimit setting to settings.json */
	saveCompactionTokenLimit: (value: number) => typedError<null, string>(__TAURI_INVOKE("save_compaction_token_limit", { value })),
	/**
	 *  Gets the compactionTokenLimitPerModel setting from settings.json
	 *  Returns empty map by default if not set
	 */
	getCompactionTokenLimitPerModel: () => typedError<{ [key in string]: number }, string>(__TAURI_INVOKE("get_compaction_token_limit_per_model")),
	/**  Saves the compactionTokenLimitPerModel setting to settings.json */
	saveCompactionTokenLimitPerModel: (overrides: { [key in string]: number }) => typedError<null, string>(__TAURI_INVOKE("save_compaction_token_limit_per_model", { overrides })),
	/**
	 *  Loads all channels from ~/.droidgear/channels.json
	 *  Falls back to ~/.factory/settings.json for migration
	 */
	loadChannels: () => typedError<ApiChannel[], string>(__TAURI_INVOKE("load_channels")),
	/**  Saves all channels to ~/.droidgear/channels.json */
	saveChannels: (channels: ApiChannel[]) => typedError<null, string>(__TAURI_INVOKE("save_channels", { channels })),
	/**  Saves a channel's credentials to ~/.droidgear/auth/ */
	saveChannelCredentials: (channelId: string, username: string, password: string) => typedError<null, string>(__TAURI_INVOKE("save_channel_credentials", { channelId, username, password })),
	/**  Gets a channel's credentials from ~/.droidgear/auth/ */
	getChannelCredentials: (channelId: string) => typedError<[string, string] | null, string>(__TAURI_INVOKE("get_channel_credentials", { channelId })),
	/**  Saves a channel's API key to ~/.droidgear/auth/ */
	saveChannelApiKey: (channelId: string, apiKey: string) => typedError<null, string>(__TAURI_INVOKE("save_channel_api_key", { channelId, apiKey })),
	/**  Gets a channel's API key from ~/.droidgear/auth/ */
	getChannelApiKey: (channelId: string) => typedError<string | null, string>(__TAURI_INVOKE("get_channel_api_key", { channelId })),
	/**  Deletes a channel's credentials from ~/.droidgear/auth/ */
	deleteChannelCredentials: (channelId: string) => typedError<null, string>(__TAURI_INVOKE("delete_channel_credentials", { channelId })),
	/**  Detects channel type by probing characteristic endpoints */
	detectChannelType: (baseUrl: string) => typedError<ChannelType, string>(__TAURI_INVOKE("detect_channel_type", { baseUrl })),
	/**  Fetches tokens from a channel (dispatches based on channel type) */
	fetchChannelTokens: (channelType: ChannelType, baseUrl: string, username: string, password: string) => typedError<ChannelToken[], string>(__TAURI_INVOKE("fetch_channel_tokens", { channelType, baseUrl, username, password })),
	/**  Fetches models using an API key (for quick model addition from channels) */
	fetchModelsByApiKey: (baseUrl: string, apiKey: string, platform: string | null) => typedError<ModelInfo[], string>(__TAURI_INVOKE("fetch_models_by_api_key", { baseUrl, apiKey, platform })),
	/**  Load all export templates from config file. */
	loadExportTemplates: () => typedError<ExportTemplate_Serialize[], string>(__TAURI_INVOKE("load_export_templates")),
	/**
	 *  Save (create or update) an export template by name.
	 *  If a template with the same name exists, it's replaced.
	 */
	saveExportTemplate: (template: ExportTemplate) => typedError<null, string>(__TAURI_INVOKE("save_export_template", { template })),
	/**  Delete an export template by name. */
	deleteExportTemplate: (name: string) => typedError<null, string>(__TAURI_INVOKE("delete_export_template", { name })),
	/**  Run an export template by name and return the result. */
	runExportTemplate: (name: string) => typedError<ExportResult_Serialize, string>(__TAURI_INVOKE("run_export_template", { name })),
	/**
	 *  Gets the value of an environment variable.
	 *  Returns None if the variable is not set.
	 */
	getEnvVar: (name: string) => __TAURI_INVOKE<string | null>("get_env_var", { name }),
	/**
	 *  Sets an environment variable for the current process.
	 *  Note: This only affects the current process, not the system or shell.
	 */
	setEnvVar: (name: string, value: string) => __TAURI_INVOKE<void>("set_env_var", { name, value }),
	/**
	 *  Removes an environment variable from the current process.
	 *  Note: This only affects the current process, not the system or shell.
	 */
	removeEnvVar: (name: string) => __TAURI_INVOKE<void>("remove_env_var", { name }),
	/**
	 *  Gets environment variables from a login shell.
	 *  This is useful for GUI apps that don't inherit shell environment.
	 *  Uses non-interactive mode for faster execution.
	 */
	getShellEnv: () => typedError<{ [key in string]: string }, string>(__TAURI_INVOKE("get_shell_env")),
	/**  Lists all spec files from ~/.factory/specs directory. */
	listSpecs: () => typedError<SpecFile[], string>(__TAURI_INVOKE("list_specs")),
	/**  Reads a single spec file by path. */
	readSpec: (path: string) => typedError<SpecFile, string>(__TAURI_INVOKE("read_spec", { path })),
	/**  Renames a spec file. */
	renameSpec: (oldPath: string, newName: string) => typedError<SpecFile, string>(__TAURI_INVOKE("rename_spec", { oldPath, newName })),
	/**  Deletes a spec file. */
	deleteSpec: (path: string) => typedError<null, string>(__TAURI_INVOKE("delete_spec", { path })),
	/**  Updates a spec file content. */
	updateSpec: (path: string, content: string) => typedError<SpecFile, string>(__TAURI_INVOKE("update_spec", { path, content })),
	/**  Starts watching the specs directory for changes. */
	startSpecsWatcher: () => typedError<null, string>(__TAURI_INVOKE("start_specs_watcher")),
	/**  Stops watching the specs directory. */
	stopSpecsWatcher: () => typedError<null, string>(__TAURI_INVOKE("stop_specs_watcher")),
	/**  Loads all MCP servers from ~/.factory/mcp.json */
	loadMcpServers: () => typedError<McpServer_Serialize[], string>(__TAURI_INVOKE("load_mcp_servers")),
	/**  Saves an MCP server (creates or updates) */
	saveMcpServer: (server: McpServer) => typedError<null, string>(__TAURI_INVOKE("save_mcp_server", { server })),
	/**  Deletes an MCP server by name */
	deleteMcpServer: (name: string) => typedError<null, string>(__TAURI_INVOKE("delete_mcp_server", { name })),
	/**  Toggles an MCP server's disabled state */
	toggleMcpServer: (name: string, disabled: boolean) => typedError<null, string>(__TAURI_INVOKE("toggle_mcp_server", { name, disabled })),
	/**  List all Claude Code profiles */
	listClaudeProfiles: () => typedError<ClaudeCodeProfile_Serialize[], string>(__TAURI_INVOKE("list_claude_profiles")),
	/**  Get a profile by ID */
	getClaudeProfile: (id: string) => typedError<ClaudeCodeProfile_Serialize, string>(__TAURI_INVOKE("get_claude_profile", { id })),
	/**  Save a profile (create or update) */
	saveClaudeProfile: (profile: ClaudeCodeProfile) => typedError<null, string>(__TAURI_INVOKE("save_claude_profile", { profile })),
	/**  Delete a profile */
	deleteClaudeProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("delete_claude_profile", { id })),
	/**  Duplicate a profile */
	duplicateClaudeProfile: (id: string, newName: string) => typedError<ClaudeCodeProfile_Serialize, string>(__TAURI_INVOKE("duplicate_claude_profile", { id, newName })),
	/**  Create default profile (when no profiles exist) */
	createDefaultClaudeProfile: () => typedError<ClaudeCodeProfile_Serialize, string>(__TAURI_INVOKE("create_default_claude_profile")),
	/**  Get active profile ID */
	getActiveClaudeProfileId: () => typedError<string | null, string>(__TAURI_INVOKE("get_active_claude_profile_id")),
	/**  Set active profile ID */
	setActiveClaudeProfileId: (id: string) => typedError<null, string>(__TAURI_INVOKE("set_active_claude_profile_id", { id })),
	/**  Apply a profile to `~/.claude/settings.json` */
	applyClaudeProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("apply_claude_profile", { id })),
	/**  Get Claude Code config status */
	getClaudeConfigStatus: () => typedError<ClaudeConfigStatus_Serialize, string>(__TAURI_INVOKE("get_claude_config_status")),
	/**  Read current Claude Code configuration from settings.json */
	readClaudeCurrentConfig: () => typedError<ClaudeCurrentConfig_Serialize, string>(__TAURI_INVOKE("read_claude_current_config")),
	/**  Build the temporary-run launch plan preview for a Claude Code profile. */
	getClaudeTemporaryRunPlan: (id: string) => typedError<ClaudeTemporaryRunPlan, string>(__TAURI_INVOKE("get_claude_temporary_run_plan", { id })),
	/**  Launch Claude Code using a runtime settings overlay instead of mutating live config. */
	launchClaude: (id: string, cwd: string | null) => typedError<null, string>(__TAURI_INVOKE("launch_claude", { id, cwd })),
	/**  Lists every Claude settings file (global + custom). */
	listClaudeSettingsFiles: () => typedError<ClaudeSettingsFileInfo[], string>(__TAURI_INVOKE("list_claude_settings_files")),
	/**  Gets the currently active settings file info. */
	getActiveClaudeSettingsFile: () => typedError<ClaudeSettingsFileInfo, string>(__TAURI_INVOKE("get_active_claude_settings_file")),
	/**  Sets the active settings file. Pass null or empty string to switch to Global. */
	setActiveClaudeSettingsFile: (name: string | null) => typedError<ClaudeSettingsFileInfo, string>(__TAURI_INVOKE("set_active_claude_settings_file", { name })),
	/**  Creates a new custom settings file. */
	createClaudeSettingsFile: (name: string, copyFromActive: boolean) => typedError<ClaudeSettingsFileInfo, string>(__TAURI_INVOKE("create_claude_settings_file", { name, copyFromActive })),
	/**  Deletes a custom settings file. The Global file cannot be deleted. */
	deleteClaudeSettingsFile: (name: string) => typedError<null, string>(__TAURI_INVOKE("delete_claude_settings_file", { name })),
	/**  Reads the raw JSON object stored in a settings file (by display name). */
	readClaudeSettingsFile: (name: string) => typedError<Value, string>(__TAURI_INVOKE("read_claude_settings_file", { name })),
	/**  Persists the given JSON object as the named settings file (by display name). */
	saveClaudeSettingsFile: (name: string, contents: Value) => typedError<null, string>(__TAURI_INVOKE("save_claude_settings_file", { name, contents })),
	/**  Duplicates a settings file to a new name and activates it. */
	duplicateClaudeSettingsFile: (name: string, newName: string) => typedError<ClaudeSettingsFileInfo, string>(__TAURI_INVOKE("duplicate_claude_settings_file", { name, newName })),
	/**  Merges the contents of a settings file into the global settings.json. */
	mergeClaudeSettingsToGlobal: (name: string) => typedError<null, string>(__TAURI_INVOKE("merge_claude_settings_to_global", { name })),
	/**  Loads the current global settings.json content into a settings file. */
	loadClaudeSettingsFromLive: (name: string) => typedError<null, string>(__TAURI_INVOKE("load_claude_settings_from_live", { name })),
	/**
	 *  Returns a shell command string preview for launching Claude with the
	 *  active settings file. Useful for the "copy command" fallback.
	 */
	getClaudeSettingsLaunchCommand: (skipDangerous: boolean) => typedError<[string, string], string>(__TAURI_INVOKE("get_claude_settings_launch_command", { skipDangerous })),
	/**  Plan a temporary run using a named settings file (instead of a profile). */
	planClaudeTemporaryRunFromFile: (name: string) => typedError<ClaudeTemporaryRunPlan, string>(__TAURI_INVOKE("plan_claude_temporary_run_from_file", { name })),
	/**  Debug-preview a temporary run from a named settings file. */
	previewClaudeTemporaryRunFromFile: (name: string) => typedError<ClaudeTemporaryRunDebugPreview_Serialize, string>(__TAURI_INVOKE("preview_claude_temporary_run_from_file", { name })),
	/**
	 *  Launches Claude Code in a terminal using the active settings file. The
	 *  settings file is copied into a runtime-private directory so the live
	 *  configuration is never mutated. When `skip_dangerous` is true the
	 *  `--dangerously-skip-permissions` flag is appended.
	 */
	launchClaudeWithSettings: (cwd: string | null, skipDangerous: boolean) => typedError<null, string>(__TAURI_INVOKE("launch_claude_with_settings", { cwd, skipDangerous })),
	/**  List all Codex profiles */
	listCodexProfiles: () => typedError<CodexProfile_Serialize[], string>(__TAURI_INVOKE("list_codex_profiles")),
	/**  Get a profile by ID */
	getCodexProfile: (id: string) => typedError<CodexProfile_Serialize, string>(__TAURI_INVOKE("get_codex_profile", { id })),
	/**  Save a profile (create or update) */
	saveCodexProfile: (profile: CodexProfile) => typedError<null, string>(__TAURI_INVOKE("save_codex_profile", { profile })),
	/**  Delete a profile */
	deleteCodexProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("delete_codex_profile", { id })),
	/**  Duplicate a profile */
	duplicateCodexProfile: (id: string, newName: string) => typedError<CodexProfile_Serialize, string>(__TAURI_INVOKE("duplicate_codex_profile", { id, newName })),
	/**  Create default profile (when no profiles exist) */
	createDefaultCodexProfile: () => typedError<CodexProfile_Serialize, string>(__TAURI_INVOKE("create_default_codex_profile")),
	/**  Get active profile ID */
	getActiveCodexProfileId: () => typedError<string | null, string>(__TAURI_INVOKE("get_active_codex_profile_id")),
	/**  Apply a profile to `~/.codex/*` */
	applyCodexProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("apply_codex_profile", { id })),
	/**  Get Codex config status */
	getCodexConfigStatus: () => typedError<CodexConfigStatus, string>(__TAURI_INVOKE("get_codex_config_status")),
	/**  Read current Codex configuration from config files */
	readCodexCurrentConfig: () => typedError<CodexCurrentConfig_Serialize, string>(__TAURI_INVOKE("read_codex_current_config")),
	/**
	 *  Inspect the installed Codex CLI and report whether temporary-run launch-time
	 *  overrides are supported.
	 */
	getCodexCliCapability: () => typedError<CodexCliCapability, string>(__TAURI_INVOKE("get_codex_cli_capability")),
	/**  Build the zero-write temporary-run launch plan preview for a Codex profile. */
	getCodexTemporaryRunPlan: (id: string) => typedError<CodexTemporaryRunPlan, string>(__TAURI_INVOKE("get_codex_temporary_run_plan", { id })),
	/**
	 *  Prepare a one-shot in-app terminal run (runtime CODEX_HOME + env, including secrets).
	 * 
	 *  Does not open an external terminal. The frontend injects env into the PTY and
	 *  auto-executes the command. Secret values must not be persisted client-side.
	 */
	prepareCodexInAppRun: (id: string) => typedError<CodexInAppRunPlan, string>(__TAURI_INVOKE("prepare_codex_in_app_run", { id })),
	/**  Launch Codex using a runtime `CODEX_HOME` snapshot instead of mutating live config. */
	launchCodex: (id: string, cwd: string | null) => typedError<null, string>(__TAURI_INVOKE("launch_codex", { id, cwd })),
	/**  List all Hermes profiles */
	listHermesProfiles: () => typedError<HermesProfile_Serialize[], string>(__TAURI_INVOKE("list_hermes_profiles")),
	/**  Get a profile by ID */
	getHermesProfile: (id: string) => typedError<HermesProfile_Serialize, string>(__TAURI_INVOKE("get_hermes_profile", { id })),
	/**  Save a profile (create or update) */
	saveHermesProfile: (profile: HermesProfile) => typedError<null, string>(__TAURI_INVOKE("save_hermes_profile", { profile })),
	/**  Delete a profile */
	deleteHermesProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("delete_hermes_profile", { id })),
	/**  Duplicate a profile */
	duplicateHermesProfile: (id: string, newName: string) => typedError<HermesProfile_Serialize, string>(__TAURI_INVOKE("duplicate_hermes_profile", { id, newName })),
	/**  Create default profile (when no profiles exist) */
	createDefaultHermesProfile: () => typedError<HermesProfile_Serialize, string>(__TAURI_INVOKE("create_default_hermes_profile")),
	/**  Get active profile ID */
	getActiveHermesProfileId: () => typedError<string | null, string>(__TAURI_INVOKE("get_active_hermes_profile_id")),
	/**  Apply a profile to `~/.hermes/config.yaml` */
	applyHermesProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("apply_hermes_profile", { id })),
	/**  Get Hermes config status */
	getHermesConfigStatus: () => typedError<HermesConfigStatus, string>(__TAURI_INVOKE("get_hermes_config_status")),
	/**  Read current Hermes configuration from config files */
	readHermesCurrentConfig: () => typedError<HermesCurrentConfig_Serialize, string>(__TAURI_INVOKE("read_hermes_current_config")),
	/**  List all Pi profiles */
	listPiProfiles: () => typedError<PiProfile_Serialize[], string>(__TAURI_INVOKE("list_pi_profiles")),
	/**  Get a profile by ID */
	getPiProfile: (id: string) => typedError<PiProfile_Serialize, string>(__TAURI_INVOKE("get_pi_profile", { id })),
	/**  Save a profile (create or update) */
	savePiProfile: (profile: PiProfile) => typedError<null, string>(__TAURI_INVOKE("save_pi_profile", { profile })),
	/**  Delete a profile */
	deletePiProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("delete_pi_profile", { id })),
	/**  Duplicate a profile */
	duplicatePiProfile: (id: string, newName: string) => typedError<PiProfile_Serialize, string>(__TAURI_INVOKE("duplicate_pi_profile", { id, newName })),
	/**  Create default profile (when no profiles exist) */
	createDefaultPiProfile: () => typedError<PiProfile_Serialize, string>(__TAURI_INVOKE("create_default_pi_profile")),
	/**  Get active profile ID */
	getActivePiProfileId: () => typedError<string | null, string>(__TAURI_INVOKE("get_active_pi_profile_id")),
	/**  Set active profile ID */
	setActivePiProfileId: (id: string) => typedError<null, string>(__TAURI_INVOKE("set_active_pi_profile_id", { id })),
	/**  Apply a profile to `~/.pi/agent/models.json` */
	applyPiProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("apply_pi_profile", { id })),
	/**  Get Pi config status */
	getPiConfigStatus: () => typedError<PiConfigStatus, string>(__TAURI_INVOKE("get_pi_config_status")),
	/**  Read current Pi configuration from config files */
	readPiCurrentConfig: () => typedError<PiCurrentConfig_Serialize, string>(__TAURI_INVOKE("read_pi_current_config")),
	/**  List all OMP profiles */
	listOmpProfiles: () => typedError<OmpProfile_Serialize[], string>(__TAURI_INVOKE("list_omp_profiles")),
	/**  Get a profile by ID */
	getOmpProfile: (id: string) => typedError<OmpProfile_Serialize, string>(__TAURI_INVOKE("get_omp_profile", { id })),
	/**  Save a profile (create or update) */
	saveOmpProfile: (profile: OmpProfile) => typedError<null, string>(__TAURI_INVOKE("save_omp_profile", { profile })),
	/**  Delete a profile */
	deleteOmpProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("delete_omp_profile", { id })),
	/**  Duplicate a profile */
	duplicateOmpProfile: (id: string, newName: string) => typedError<OmpProfile_Serialize, string>(__TAURI_INVOKE("duplicate_omp_profile", { id, newName })),
	/**  Create default profile (when no profiles exist) */
	createDefaultOmpProfile: () => typedError<OmpProfile_Serialize, string>(__TAURI_INVOKE("create_default_omp_profile")),
	/**  Get active profile ID */
	getActiveOmpProfileId: () => typedError<string | null, string>(__TAURI_INVOKE("get_active_omp_profile_id")),
	/**  Set active profile ID */
	setActiveOmpProfileId: (id: string) => typedError<null, string>(__TAURI_INVOKE("set_active_omp_profile_id", { id })),
	/**  Apply a profile to `~/.omp/agent/models.yml` */
	applyOmpProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("apply_omp_profile", { id })),
	/**  Get OMP config status */
	getOmpConfigStatus: () => typedError<OmpConfigStatus, string>(__TAURI_INVOKE("get_omp_config_status")),
	/**  Read current OMP configuration from config files */
	readOmpCurrentConfig: () => typedError<OmpCurrentConfig_Serialize, string>(__TAURI_INVOKE("read_omp_current_config")),
	/**  Test an OMP provider connection via HTTP */
	testOmpProviderConnection: (providerId: string) => typedError<OmpProviderTestResult_Serialize, string>(__TAURI_INVOKE("test_omp_provider_connection", { providerId })),
	/**  Test a Pi provider using an isolated temporary models.json and Pi CLI run. */
	testPiProviderConnection: (providerId: string, config: PiProviderConfig) => typedError<PiProviderTestResult_Serialize, string>(__TAURI_INVOKE("test_pi_provider_connection", { providerId, config })),
	listPiSessions: () => typedError<PiSessionSummary[], string>(__TAURI_INVOKE("list_pi_sessions")),
	getPiSessionDetail: (sessionPath: string) => typedError<PiSessionDetail_Serialize, string>(__TAURI_INVOKE("get_pi_session_detail", { sessionPath })),
	deletePiSession: (sessionPath: string) => typedError<null, string>(__TAURI_INVOKE("delete_pi_session", { sessionPath })),
	startPiSessionsWatcher: () => typedError<null, string>(__TAURI_INVOKE("start_pi_sessions_watcher")),
	stopPiSessionsWatcher: () => typedError<null, string>(__TAURI_INVOKE("stop_pi_sessions_watcher")),
	/**  List all OpenCode profiles */
	listOpencodeProfiles: () => typedError<OpenCodeProfile_Serialize[], string>(__TAURI_INVOKE("list_opencode_profiles")),
	/**  Get a profile by ID */
	getOpencodeProfile: (id: string) => typedError<OpenCodeProfile_Serialize, string>(__TAURI_INVOKE("get_opencode_profile", { id })),
	/**  Save a profile (create or update) */
	saveOpencodeProfile: (profile: OpenCodeProfile) => typedError<null, string>(__TAURI_INVOKE("save_opencode_profile", { profile })),
	/**  Delete a profile */
	deleteOpencodeProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("delete_opencode_profile", { id })),
	/**  Duplicate a profile */
	duplicateOpencodeProfile: (id: string, newName: string) => typedError<OpenCodeProfile_Serialize, string>(__TAURI_INVOKE("duplicate_opencode_profile", { id, newName })),
	/**  Create default profile if none exists */
	createDefaultProfile: () => typedError<OpenCodeProfile_Serialize, string>(__TAURI_INVOKE("create_default_profile")),
	/**  Get active profile ID */
	getActiveOpencodeProfileId: () => typedError<string | null, string>(__TAURI_INVOKE("get_active_opencode_profile_id")),
	/**  Apply a profile to OpenCode config files */
	applyOpencodeProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("apply_opencode_profile", { id })),
	/**  Get OpenCode config status */
	getOpencodeConfigStatus: () => typedError<OpenCodeConfigStatus, string>(__TAURI_INVOKE("get_opencode_config_status")),
	/**  Get provider templates */
	getOpencodeProviderTemplates: () => typedError<ProviderTemplate_Serialize[], string>(__TAURI_INVOKE("get_opencode_provider_templates")),
	/**  Test provider connection */
	testOpencodeProviderConnection: (providerId: string, baseUrl: string, apiKey: string) => typedError<boolean, string>(__TAURI_INVOKE("test_opencode_provider_connection", { providerId, baseUrl, apiKey })),
	/**  Read current OpenCode configuration from config files */
	readOpencodeCurrentConfig: () => typedError<OpenCodeCurrentConfig_Serialize, string>(__TAURI_INVOKE("read_opencode_current_config")),
	/**  List all OpenClaw profiles */
	listOpenclawProfiles: () => typedError<OpenClawProfile_Serialize[], string>(__TAURI_INVOKE("list_openclaw_profiles")),
	/**  Get a profile by ID */
	getOpenclawProfile: (id: string) => typedError<OpenClawProfile_Serialize, string>(__TAURI_INVOKE("get_openclaw_profile", { id })),
	/**  Save a profile (create or update) */
	saveOpenclawProfile: (profile: OpenClawProfile) => typedError<null, string>(__TAURI_INVOKE("save_openclaw_profile", { profile })),
	/**  Delete a profile */
	deleteOpenclawProfile: (id: string) => typedError<null, string>(__TAURI_INVOKE("delete_openclaw_profile", { id })),
	/**  Duplicate a profile */
	duplicateOpenclawProfile: (id: string, newName: string) => typedError<OpenClawProfile_Serialize, string>(__TAURI_INVOKE("duplicate_openclaw_profile", { id, newName })),
	/**  Create default profile (when no profiles exist) */
	createDefaultOpenclawProfile: () => typedError<OpenClawProfile_Serialize, string>(__TAURI_INVOKE("create_default_openclaw_profile")),
	/**  Get active profile ID */
	getActiveOpenclawProfileId: () => typedError<string | null, string>(__TAURI_INVOKE("get_active_openclaw_profile_id")),
	/**  Apply a profile to `~/.openclaw/openclaw.json` */
	applyOpenclawProfile: (profile: OpenClawProfile) => typedError<null, string>(__TAURI_INVOKE("apply_openclaw_profile", { profile })),
	/**  Get OpenClaw config status */
	getOpenclawConfigStatus: () => typedError<OpenClawConfigStatus, string>(__TAURI_INVOKE("get_openclaw_config_status")),
	/**  Read current OpenClaw configuration from config file */
	readOpenclawCurrentConfig: () => typedError<OpenClawCurrentConfig_Serialize, string>(__TAURI_INVOKE("read_openclaw_current_config")),
	/**  Read subagents from OpenClaw config file */
	readOpenclawSubagents: () => typedError<OpenClawSubAgent_Serialize[], string>(__TAURI_INVOKE("read_openclaw_subagents")),
	/**  Save subagents to OpenClaw config file */
	saveOpenclawSubagents: (subagents: OpenClawSubAgent[]) => typedError<null, string>(__TAURI_INVOKE("save_openclaw_subagents", { subagents })),
	/**  Read the current Dsh providers from `~/.dsh/settings.yaml`. */
	readDshCurrentConfig: () => typedError<DshCurrentConfig_Serialize, string>(__TAURI_INVOKE("read_dsh_current_config")),
	/**  Insert or update one provider in `llm-pi-ai.providers`. */
	saveDshProvider: (providerId: string, config: DshProviderConfig) => typedError<null, string>(__TAURI_INVOKE("save_dsh_provider", { providerId, config })),
	/**  Remove one provider from `llm-pi-ai.providers`. */
	deleteDshProvider: (providerId: string) => typedError<null, string>(__TAURI_INVOKE("delete_dsh_provider", { providerId })),
	/**  Get the status of `~/.dsh/settings.yaml`. */
	getDshConfigStatus: () => typedError<DshConfigStatus, string>(__TAURI_INVOKE("get_dsh_config_status")),
	/**  Read env-var → API key refs from `~/.dsh/.credentials.yaml`. */
	readDshCredentials: () => typedError<DshCredentials_Serialize, string>(__TAURI_INVOKE("read_dsh_credentials")),
	/**
	 *  Insert or update one credential ref (env var name → value) in
	 *  `~/.dsh/.credentials.yaml`. An empty value removes the entry.
	 */
	saveDshCredentialRef: (name: string, value: string) => typedError<null, string>(__TAURI_INVOKE("save_dsh_credential_ref", { name, value })),
	/**  Remove one credential ref from `~/.dsh/.credentials.yaml`. */
	deleteDshCredentialRef: (name: string) => typedError<null, string>(__TAURI_INVOKE("delete_dsh_credential_ref", { name })),
	/**
	 *  Fetch the model list from a provider's `/{baseURL}/models` endpoint using
	 *  the given API key, with registry metadata enrichment (reasoningEfforts,
	 *  contextWindow, maxTokens, name).
	 */
	fetchDshModels: (baseUrl: string, apiKey: string, api: string | null) => typedError<DshModel_Serialize[], string>(__TAURI_INVOKE("fetch_dsh_models", { baseUrl, apiKey, api })),
	/**  Lists all session projects from ~/.factory/sessions directory. */
	listSessionProjects: () => typedError<SessionProject[], string>(__TAURI_INVOKE("list_session_projects")),
	/**  Lists all sessions, optionally filtered by project. */
	listSessions: (project: string | null) => typedError<SessionSummary[], string>(__TAURI_INVOKE("list_sessions", { project })),
	/**  Gets detailed session information including messages. */
	getSessionDetail: (sessionPath: string) => typedError<SessionDetail_Serialize, string>(__TAURI_INVOKE("get_session_detail", { sessionPath })),
	/**  Starts watching the sessions directory for changes. */
	startSessionsWatcher: () => typedError<null, string>(__TAURI_INVOKE("start_sessions_watcher")),
	/**  Stops watching the sessions directory. */
	stopSessionsWatcher: () => typedError<null, string>(__TAURI_INVOKE("stop_sessions_watcher")),
	/**  Deletes a session by removing its .jsonl and .settings.json files. */
	deleteSession: (sessionPath: string) => typedError<null, string>(__TAURI_INVOKE("delete_session", { sessionPath })),
	testModelConnection: (modelId: string) => typedError<ModelTestResult_Serialize, string>(__TAURI_INVOKE("test_model_connection", { modelId })),
	testAllModelConnectionsCommand: () => typedError<ModelTestResult_Serialize[], string>(__TAURI_INVOKE("test_all_model_connections_command")),
	getConnectivitySummary: (results: ModelTestResult[]) => typedError<ConnectivitySummary, string>(__TAURI_INVOKE("get_connectivity_summary", { results })),
	testProviderConnection: (provider: string, baseUrl: string, apiKey: string, modelId: string) => typedError<ConnectionDiagnostics_Serialize, string>(__TAURI_INVOKE("test_provider_connection", { provider, baseUrl, apiKey, modelId })),
	testModelConnectionWithMode: (modelId: string, mode: TestMode, prompt: string | null) => typedError<ModelTestResult_Serialize, string>(__TAURI_INVOKE("test_model_connection_with_mode", { modelId, mode, prompt })),
	testAllModelConnectionsWithMode: (mode: TestMode, prompt: string | null) => typedError<ModelTestResult_Serialize[], string>(__TAURI_INVOKE("test_all_model_connections_with_mode", { mode, prompt })),
	/**  Gets the current configuration paths (custom values only) */
	getConfigPaths: () => typedError<ConfigPaths_Serialize, string>(__TAURI_INVOKE("get_config_paths")),
	/**  Gets all effective paths with default indicators */
	getEffectivePaths: () => typedError<EffectivePaths, string>(__TAURI_INVOKE("get_effective_paths")),
	/**  Saves a single configuration path */
	saveConfigPath: (key: string, path: string) => typedError<null, string>(__TAURI_INVOKE("save_config_path", { key, path })),
	/**  Resets a single configuration path to default */
	resetConfigPath: (key: string) => typedError<null, string>(__TAURI_INVOKE("reset_config_path", { key })),
	/**  Gets the default paths (for UI display) */
	getDefaultPaths: () => typedError<EffectivePaths, string>(__TAURI_INVOKE("get_default_paths")),
	/**  Checks if WSL is available and lists distributions (Windows only) */
	getWslInfo: () => typedError<WslInfo, string>(__TAURI_INVOKE("get_wsl_info")),
	/**  Gets the WSL username for a specific distribution */
	getWslUsername: (distro: string) => typedError<string, string>(__TAURI_INVOKE("get_wsl_username", { distro })),
	/**  Builds a WSL path for a config directory */
	buildWslPath: (distro: string, username: string, configKey: string) => typedError<string, string>(__TAURI_INVOKE("build_wsl_path", { distro, username, configKey })),
	getUpdateChannel: () => typedError<UpdateChannel, string>(__TAURI_INVOKE("get_update_channel")),
	checkPortableUpdate: () => typedError<PortableUpdateInfo | null, string>(__TAURI_INVOKE("check_portable_update")),
	installPortableUpdate: (update: PortableUpdateInfo) => typedError<null, string>(__TAURI_INVOKE("install_portable_update", { update })),
	/**  Lists all available Droid settings files (global + custom) */
	listDroidSettingsFiles: () => typedError<SettingsFileInfo[], string>(__TAURI_INVOKE("list_droid_settings_files")),
	/**  Gets the currently active settings file info */
	getActiveDroidSettingsFile: () => typedError<SettingsFileInfo, string>(__TAURI_INVOKE("get_active_droid_settings_file")),
	/**  Sets the active settings file. Pass null or empty to switch to Global. */
	setActiveDroidSettingsFile: (name: string | null) => typedError<SettingsFileInfo, string>(__TAURI_INVOKE("set_active_droid_settings_file", { name })),
	/**  Creates a new settings file. If copy_from_active is true, copies from the active file. */
	createDroidSettingsFile: (name: string, copyFromActive: boolean) => typedError<SettingsFileInfo, string>(__TAURI_INVOKE("create_droid_settings_file", { name, copyFromActive })),
	/**  Deletes a custom settings file. Cannot delete the global file. */
	deleteDroidSettingsFile: (name: string) => typedError<null, string>(__TAURI_INVOKE("delete_droid_settings_file", { name })),
	/**
	 *  Gets the launch command for Droid with the active settings file.
	 *  Returns [command_string, settings_path].
	 */
	getDroidLaunchCommand: () => typedError<[string, string], string>(__TAURI_INVOKE("get_droid_launch_command")),
	/**
	 *  Launches Droid CLI in a terminal with the active settings file.
	 *  Respects the user's preferredTerminal preference.
	 *  If `cwd` is provided, the terminal will start in that directory.
	 */
	launchDroid: (cwd: string | null) => typedError<null, string>(__TAURI_INVOKE("launch_droid", { cwd })),
	/**  Lists trusted folders from the global Factory settings file. */
	listDroidTrustedFolders: () => typedError<TrustedFolder[], string>(__TAURI_INVOKE("list_droid_trusted_folders")),
	/**  Adds a directory to Droid's global trusted-folder list. */
	addDroidTrustedFolder: (path: string) => typedError<TrustedFolder, string>(__TAURI_INVOKE("add_droid_trusted_folder", { path })),
	/**  Removes a directory from Droid's global trusted-folder list. */
	removeDroidTrustedFolder: (path: string) => typedError<null, string>(__TAURI_INVOKE("remove_droid_trusted_folder", { path })),
	/**  Removes multiple directories from Droid's global trusted-folder list. */
	removeDroidTrustedFolders: (paths: string[]) => typedError<null, string>(__TAURI_INVOKE("remove_droid_trusted_folders", { paths })),
	listFactoryAuthProfiles: () => typedError<AuthProfileState, string>(__TAURI_INVOKE("list_factory_auth_profiles")),
	getActiveFactoryAuthProfile: () => typedError<string | null, string>(__TAURI_INVOKE("get_active_factory_auth_profile")),
	switchFactoryAuthProfile: (name: string) => typedError<null, string>(__TAURI_INVOKE("switch_factory_auth_profile", { name })),
	saveCurrentFactoryAuthProfile: (name: string, label: string) => typedError<null, string>(__TAURI_INVOKE("save_current_factory_auth_profile", { name, label })),
	deleteFactoryAuthProfile: (name: string) => typedError<null, string>(__TAURI_INVOKE("delete_factory_auth_profile", { name })),
	renameFactoryAuthProfile: (name: string, label: string) => typedError<null, string>(__TAURI_INVOKE("rename_factory_auth_profile", { name, label })),
	/**  List all Codex auth profiles */
	listCodexAuthProfiles: () => typedError<CodexAuthProfileState_Serialize, string>(__TAURI_INVOKE("list_codex_auth_profiles")),
	/**  Check if current auth is official */
	isCodexOfficialAuth: () => typedError<boolean, string>(__TAURI_INVOKE("is_codex_official_auth")),
	/**  Save current auth.json as a named profile */
	saveCurrentCodexAuthProfile: (name: string, label: string) => typedError<null, string>(__TAURI_INVOKE("save_current_codex_auth_profile", { name, label })),
	/**  Switch to a saved auth profile */
	switchCodexAuthProfile: (name: string) => typedError<null, string>(__TAURI_INVOKE("switch_codex_auth_profile", { name })),
	/**  Delete a saved auth profile */
	deleteCodexAuthProfile: (name: string) => typedError<null, string>(__TAURI_INVOKE("delete_codex_auth_profile", { name })),
	/**  Rename a saved auth profile's label */
	renameCodexAuthProfile: (name: string, label: string) => typedError<null, string>(__TAURI_INVOKE("rename_codex_auth_profile", { name, label })),
	/**  Detect auth mode conflict between current auth and a profile */
	detectCodexAuthConflict: (profileName: string) => typedError<CodexAuthConflictInfo, string>(__TAURI_INVOKE("detect_codex_auth_conflict", { profileName })),
	/**  Detect auth mode conflict when applying a CodexProfile (not an auth profile) */
	detectCodexApplyAuthConflict: (codexProfileId: string) => typedError<CodexAuthConflictInfo, string>(__TAURI_INVOKE("detect_codex_apply_auth_conflict", { codexProfileId })),
	/**  Lists all Codex sessions from `<codex home>/sessions`. */
	listCodexSessions: () => typedError<CodexSessionSummary[], string>(__TAURI_INVOKE("list_codex_sessions")),
	/**  Lists all Codex model providers aggregated from every configured profile. */
	listCodexSessionProviders: () => typedError<CodexSessionProvider[], string>(__TAURI_INVOKE("list_codex_session_providers")),
	/**  Gets detailed Codex session information including messages. */
	getCodexSessionDetail: (sessionPath: string) => typedError<CodexSessionDetail_Serialize, string>(__TAURI_INVOKE("get_codex_session_detail", { sessionPath })),
	/**  Deletes a Codex session by removing its .jsonl file. */
	deleteCodexSession: (sessionPath: string) => typedError<null, string>(__TAURI_INVOKE("delete_codex_session", { sessionPath })),
	/**  Rewrites the `model_provider` of a session file's `session_meta` line. */
	setCodexSessionProvider: (sessionPath: string, providerId: string) => typedError<null, string>(__TAURI_INVOKE("set_codex_session_provider", { sessionPath, providerId })),
	/**  Starts watching the codex sessions directory for changes. */
	startCodexSessionsWatcher: () => typedError<null, string>(__TAURI_INVOKE("start_codex_sessions_watcher")),
	/**  Stops watching the codex sessions directory. */
	stopCodexSessionsWatcher: () => typedError<null, string>(__TAURI_INVOKE("stop_codex_sessions_watcher")),
	/**
	 *  Tauri command: snap the main window to the default size, clear fullscreen,
	 *  re-center, and quarantine the saved state file so the next launch starts
	 *  fresh. Surfaced via the application menu and a global shortcut so users
	 *  can recover from an unmanageable window without editing files by hand.
	 */
	resetWindowState: () => typedError<null, string>(__TAURI_INVOKE("reset_window_state")),
};

/* Types */
/**  Channel configuration */
export type ApiChannel = {
	/**  Unique identifier (UUID) */
	id: string,
	/**  User-defined name */
	name: string,
	/**  Channel type */
	type: ChannelType,
	/**  API base URL */
	baseUrl: string,
	/**  Whether the channel is enabled */
	enabled: boolean,
	/**  Creation timestamp (milliseconds) - use f64 for JS compatibility */
	createdAt: number | null,
};

/**
 *  Application preferences that persist to disk.
 *  Only contains settings that should be saved between sessions.
 */
export type AppPreferences = AppPreferences_Serialize | AppPreferences_Deserialize;

/**
 *  Application preferences that persist to disk.
 *  Only contains settings that should be saved between sessions.
 */
export type AppPreferences_Deserialize = {
	theme: string,
	/**
	 *  User's preferred language (e.g., "en", "es", "de")
	 *  If None, uses system locale detection
	 */
	language: string | null,
	/**
	 *  Custom font family for terminal (e.g., "Cascadia Mono NF")
	 *  If None, uses default monospace fonts
	 */
	terminal_font_family?: string | null,
	/**
	 *  Custom shell command for terminal (Windows only, e.g., "cmd.exe", "pwsh.exe")
	 *  If None, uses default PowerShell on Windows
	 */
	terminal_shell_command?: string | null,
	/**
	 *  Whether to disable automatic update checks at startup
	 *  If None, defaults to false (auto-update enabled)
	 */
	disable_auto_update?: boolean | null,
	/**
	 *  Preferred terminal app for launching Droid CLI
	 *  "system-default" | "terminal" | "iterm2" | "ghostty" (macOS)
	 *  "auto-detect" | "gnome-terminal" | "konsole" | "xfce4-terminal" | "x-terminal-emulator" | custom (Linux)
	 *  "windows-terminal" | "cmd" | "powershell" (Windows)
	 *  If None, defaults to platform-appropriate default
	 */
	preferred_terminal?: string | null,
	/**  Droid temporary-run runtime policy. */
	droid_run?: DroidRunPreferences_Deserialize | null,
};

/**
 *  Application preferences that persist to disk.
 *  Only contains settings that should be saved between sessions.
 */
export type AppPreferences_Serialize = {
	theme: string,
	/**
	 *  User's preferred language (e.g., "en", "es", "de")
	 *  If None, uses system locale detection
	 */
	language: string | null,
	/**
	 *  Custom font family for terminal (e.g., "Cascadia Mono NF")
	 *  If None, uses default monospace fonts
	 */
	terminal_font_family: string | null,
	/**
	 *  Custom shell command for terminal (Windows only, e.g., "cmd.exe", "pwsh.exe")
	 *  If None, uses default PowerShell on Windows
	 */
	terminal_shell_command: string | null,
	/**
	 *  Whether to disable automatic update checks at startup
	 *  If None, defaults to false (auto-update enabled)
	 */
	disable_auto_update: boolean | null,
	/**
	 *  Preferred terminal app for launching Droid CLI
	 *  "system-default" | "terminal" | "iterm2" | "ghostty" (macOS)
	 *  "auto-detect" | "gnome-terminal" | "konsole" | "xfce4-terminal" | "x-terminal-emulator" | custom (Linux)
	 *  "windows-terminal" | "cmd" | "powershell" (Windows)
	 *  If None, defaults to platform-appropriate default
	 */
	preferred_terminal: string | null,
	/**  Droid temporary-run runtime policy. */
	droid_run: DroidRunPreferences_Serialize | null,
};

export type AuthProfile = {
	name: string,
	label: string,
	createdAt: string,
};

export type AuthProfileState = {
	active: string | null,
	profiles: AuthProfile[],
};

/**  Block streaming chunk configuration */
export type BlockStreamingChunk = BlockStreamingChunk_Serialize | BlockStreamingChunk_Deserialize;

/**  Block streaming chunk configuration */
export type BlockStreamingChunk_Deserialize = {
	minChars: number | null,
	maxChars: number | null,
};

/**  Block streaming chunk configuration */
export type BlockStreamingChunk_Serialize = {
	minChars?: number,
	maxChars?: number,
};

/**  Block streaming coalesce configuration */
export type BlockStreamingCoalesce = BlockStreamingCoalesce_Serialize | BlockStreamingCoalesce_Deserialize;

/**  Block streaming coalesce configuration */
export type BlockStreamingCoalesce_Deserialize = {
	idleMs: number | null,
};

/**  Block streaming coalesce configuration */
export type BlockStreamingCoalesce_Serialize = {
	idleMs?: number,
};

/**  Block streaming configuration */
export type BlockStreamingConfig = BlockStreamingConfig_Serialize | BlockStreamingConfig_Deserialize;

/**  Block streaming configuration */
export type BlockStreamingConfig_Deserialize = {
	blockStreamingDefault: string | null,
	blockStreamingBreak: string | null,
	blockStreamingChunk: BlockStreamingChunk_Deserialize | null,
	blockStreamingCoalesce: BlockStreamingCoalesce_Deserialize | null,
	telegramChannel: TelegramChannelConfig_Deserialize | null,
};

/**  Block streaming configuration */
export type BlockStreamingConfig_Serialize = {
	blockStreamingDefault?: string,
	blockStreamingBreak?: string,
	blockStreamingChunk?: BlockStreamingChunk_Serialize,
	blockStreamingCoalesce?: BlockStreamingCoalesce_Serialize,
	telegramChannel?: TelegramChannelConfig_Serialize,
};

/**  Channel filter conditions */
export type ChannelFilter = ChannelFilter_Serialize | ChannelFilter_Deserialize;

/**  Channel filter conditions */
export type ChannelFilter_Deserialize = {
	/**  Only include these channel types (empty = all) */
	types?: ChannelType[],
	/**  Only include enabled channels */
	enabledOnly?: boolean,
	/**  Only include channels with these IDs (empty = all) */
	ids?: string[],
};

/**  Channel filter conditions */
export type ChannelFilter_Serialize = {
	/**  Only include these channel types (empty = all) */
	types: ChannelType[],
	/**  Only include enabled channels */
	enabledOnly: boolean,
	/**  Only include channels with these IDs (empty = all) */
	ids: string[],
};

/**  Token from channel API */
export type ChannelToken = {
	/**  Token ID from API */
	id: number | null,
	/**  Token name */
	name: string,
	/**  Token key (sk-xxx) */
	key: string,
	/**  Status (1=enabled, 2=disabled, etc.) */
	status: number,
	/**  Remaining quota */
	remainQuota: number | null,
	/**  Used quota */
	usedQuota: number | null,
	/**  Unlimited quota flag */
	unlimitedQuota: boolean,
	/**  Platform type (openai, anthropic, gemini, etc.) - from Sub2API */
	platform: string | null,
	/**  Group name - from Sub2API */
	groupName: string | null,
};

/**  Channel types supported */
export type ChannelType = "new-api" | "sub-2-api" | "cli-proxy-api" | "ollama" | "general" | "deep-seek";

/**  Claude Code profile stored in DroidGear. */
export type ClaudeCodeProfile = ClaudeCodeProfile_Serialize | ClaudeCodeProfile_Deserialize;

/**  Claude Code profile stored in DroidGear. */
export type ClaudeCodeProfile_Deserialize = {
	id: string,
	name: string,
	description: string | null,
	baseUrl: string | null,
	bearerToken: string | null,
	model: string | null,
	smallModelUsesMainModel?: boolean,
	smallModel: string | null,
	reasoningEffort: ClaudeReasoningEffort | null,
	thinkingMode?: ClaudeThinkingMode,
	createdAt: string,
	updatedAt: string,
};

/**  Claude Code profile stored in DroidGear. */
export type ClaudeCodeProfile_Serialize = {
	id: string,
	name: string,
	description?: string,
	baseUrl?: string,
	bearerToken?: string,
	model?: string,
	smallModelUsesMainModel: boolean,
	smallModel?: string,
	reasoningEffort?: ClaudeReasoningEffort,
	thinkingMode: ClaudeThinkingMode,
	createdAt: string,
	updatedAt: string,
};

/**  Claude Code live config status. */
export type ClaudeConfigStatus = ClaudeConfigStatus_Serialize | ClaudeConfigStatus_Deserialize;

/**  Claude Code live config status. */
export type ClaudeConfigStatus_Deserialize = {
	settingsExists: boolean,
	settingsPath: string,
	configDir: string,
	parseError: string | null,
};

/**  Claude Code live config status. */
export type ClaudeConfigStatus_Serialize = {
	settingsExists: boolean,
	settingsPath: string,
	configDir: string,
	parseError?: string,
};

/**  Current Claude Code configuration read back from live settings. */
export type ClaudeCurrentConfig = ClaudeCurrentConfig_Serialize | ClaudeCurrentConfig_Deserialize;

/**  Current Claude Code configuration read back from live settings. */
export type ClaudeCurrentConfig_Deserialize = {
	baseUrl: string | null,
	bearerToken: string | null,
	model: string | null,
	smallModelUsesMainModel?: boolean,
	smallModel: string | null,
	reasoningEffort: ClaudeReasoningEffort | null,
	thinkingMode?: ClaudeThinkingMode,
};

/**  Current Claude Code configuration read back from live settings. */
export type ClaudeCurrentConfig_Serialize = {
	baseUrl?: string,
	bearerToken?: string,
	model?: string,
	smallModelUsesMainModel: boolean,
	smallModel?: string,
	reasoningEffort?: ClaudeReasoningEffort,
	thinkingMode: ClaudeThinkingMode,
};

export type ClaudeReasoningEffort = "low" | "medium" | "high" | "max";

/**  Information about a single Claude settings file. */
export type ClaudeSettingsFileInfo = {
	/**  Display name (`Global` or the filename without extension). */
	name: string,
	/**  Full path to the settings file. */
	path: string,
	/**  Whether this is the global `~/.claude/settings.json`. */
	isGlobal: boolean,
	/**  Whether this file is currently active for editing. */
	isActive: boolean,
	/**  Whether the file exists on disk. */
	exists: boolean,
};

export type ClaudeTemporaryRunDebugPreview = ClaudeTemporaryRunDebugPreview_Serialize | ClaudeTemporaryRunDebugPreview_Deserialize;

export type ClaudeTemporaryRunDebugPreview_Deserialize = {
	profileId: string,
	profileName: string,
	program: string,
	args: string[],
	childProgram: string,
	childArgs: string[],
	liveConfigDir: string,
	inheritedEnvFileSource: string | null,
	env: ([string, string])[],
	unsetEnv: string[],
	secretEnvKeys: string[],
	warnings: string[],
	settingsOverlayJson: string,
};

export type ClaudeTemporaryRunDebugPreview_Serialize = {
	profileId: string,
	profileName: string,
	program: string,
	args: string[],
	childProgram: string,
	childArgs: string[],
	liveConfigDir: string,
	inheritedEnvFileSource?: string,
	env: ([string, string])[],
	unsetEnv: string[],
	secretEnvKeys: string[],
	warnings: string[],
	settingsOverlayJson: string,
};

export type ClaudeTemporaryRunPlan = {
	program: string,
	args: string[],
	env: ([string, string])[],
	unsetEnv: string[],
	secretEnvKeys: string[],
	warnings: string[],
};

export type ClaudeThinkingMode = "inherit" | "on" | "off";

export type CodexAuthConflictInfo = {
	hasConflict: boolean,
	currentIsOfficial: boolean,
	targetIsOfficial: boolean,
};

export type CodexAuthProfile = CodexAuthProfile_Serialize | CodexAuthProfile_Deserialize;

export type CodexAuthProfileState = CodexAuthProfileState_Serialize | CodexAuthProfileState_Deserialize;

export type CodexAuthProfileState_Deserialize = {
	active: string | null,
	profiles: CodexAuthProfile_Deserialize[],
	isCurrentOfficial: boolean,
};

export type CodexAuthProfileState_Serialize = {
	active: string | null,
	profiles: CodexAuthProfile_Serialize[],
	isCurrentOfficial: boolean,
};

export type CodexAuthProfile_Deserialize = {
	name: string,
	label: string,
	createdAt: string,
	/**  Whether this profile contains official auth (has auth_mode field) */
	isOfficial: boolean,
	/**
	 *  The CodexProfile ID that was active when this auth profile was saved.
	 *  When restoring, this profile will also be applied (config.toml only).
	 */
	codexProfileId: string | null,
	/**  Live config.toml `model` snapshotted when this auth was saved. */
	model?: string | null,
	/**  Live config.toml `model_reasoning_effort` snapshotted when this auth was saved. */
	modelReasoningEffort?: string | null,
};

export type CodexAuthProfile_Serialize = {
	name: string,
	label: string,
	createdAt: string,
	/**  Whether this profile contains official auth (has auth_mode field) */
	isOfficial: boolean,
	/**
	 *  The CodexProfile ID that was active when this auth profile was saved.
	 *  When restoring, this profile will also be applied (config.toml only).
	 */
	codexProfileId?: string,
	/**  Live config.toml `model` snapshotted when this auth was saved. */
	model?: string,
	/**  Live config.toml `model_reasoning_effort` snapshotted when this auth was saved. */
	modelReasoningEffort?: string,
};

export type CodexCliCapability = {
	version: string,
	supportsConfigOverride: boolean,
};

/**  Codex Live 配置状态 */
export type CodexConfigStatus = {
	authExists: boolean,
	configExists: boolean,
	authPath: string,
	configPath: string,
};

/**  Message content block */
export type CodexContentBlock = CodexContentBlock_Serialize | CodexContentBlock_Deserialize;

/**  Message content block */
export type CodexContentBlock_Deserialize = {
	type: string,
	text: string | null,
	thinking: string | null,
};

/**  Message content block */
export type CodexContentBlock_Serialize = {
	type: string,
	text?: string,
	thinking?: string,
};

/**  当前 Codex Live 配置（从 `~/.codex/*` 读取） */
export type CodexCurrentConfig = CodexCurrentConfig_Serialize | CodexCurrentConfig_Deserialize;

/**  当前 Codex Live 配置（从 `~/.codex/*` 读取） */
export type CodexCurrentConfig_Deserialize = {
	providers?: { [key in string]: CodexProviderConfig_Deserialize },
	modelProvider: string,
	model: string,
	modelReasoningEffort: string | null,
	apiKey: string | null,
};

/**  当前 Codex Live 配置（从 `~/.codex/*` 读取） */
export type CodexCurrentConfig_Serialize = {
	providers: { [key in string]: CodexProviderConfig_Serialize },
	modelProvider: string,
	model: string,
	modelReasoningEffort?: string,
	apiKey?: string,
};

/**
 *  One-shot plan for launching Codex inside the app PTY terminal.
 * 
 *  Unlike [`CodexTemporaryRunPlan`], secret env values are included so the
 *  frontend can inject them into the PTY process environment. They must not be
 *  persisted on the client.
 */
export type CodexInAppRunPlan = {
	program: string,
	args: string[],
	/**  Public env (e.g. CODEX_HOME) plus secret API keys for PTY injection. */
	env: ([string, string])[],
	unsetEnv: string[],
	warnings: string[],
};

/**  Codex Profile（用于在 DroidGear 内部保存并切换） */
export type CodexProfile = CodexProfile_Serialize | CodexProfile_Deserialize;

/**  Codex Profile（用于在 DroidGear 内部保存并切换） */
export type CodexProfile_Deserialize = {
	id: string,
	name: string,
	description: string | null,
	createdAt: string,
	updatedAt: string,
	providers?: { [key in string]: CodexProviderConfig_Deserialize },
	modelProvider: string,
	model: string,
	modelReasoningEffort: string | null,
	apiKey: string | null,
	/**  Saved Codex auth profile name to restore on apply (openai mode only). */
	authProfileName?: string | null,
};

/**  Codex Profile（用于在 DroidGear 内部保存并切换） */
export type CodexProfile_Serialize = {
	id: string,
	name: string,
	description?: string,
	createdAt: string,
	updatedAt: string,
	providers: { [key in string]: CodexProviderConfig_Serialize },
	modelProvider: string,
	model: string,
	modelReasoningEffort?: string,
	apiKey?: string,
	/**  Saved Codex auth profile name to restore on apply (openai mode only). */
	authProfileName?: string,
};

/**  Codex Provider 配置（对应 config.toml 中的 [model_providers.<id>]） */
export type CodexProviderConfig = CodexProviderConfig_Serialize | CodexProviderConfig_Deserialize;

/**  Codex Provider 配置（对应 config.toml 中的 [model_providers.<id>]） */
export type CodexProviderConfig_Deserialize = {
	name: string | null,
	baseUrl: string | null,
	wireApi: string | null,
	requiresOpenaiAuth: boolean | null,
	envKey: string | null,
	envKeyInstructions: string | null,
	httpHeaders: { [key in string]: string } | null,
	queryParams: { [key in string]: string } | null,
	model: string | null,
	modelReasoningEffort: string | null,
	/**
	 *  DroidGear-only: written to config.toml top-level
	 *  `model_context_window` while this provider is active.
	 */
	modelContextWindow: number | null,
	/**
	 *  DroidGear-only: written to config.toml top-level
	 *  `model_auto_compact_token_limit` while this provider is active.
	 */
	modelAutoCompactTokenLimit: number | null,
	/**  Provider API key. Written to config.toml as `experimental_bearer_token`. */
	apiKey: string | null,
};

/**  Codex Provider 配置（对应 config.toml 中的 [model_providers.<id>]） */
export type CodexProviderConfig_Serialize = {
	name?: string,
	baseUrl?: string,
	wireApi?: string,
	requiresOpenaiAuth?: boolean,
	envKey?: string,
	envKeyInstructions?: string,
	httpHeaders?: { [key in string]: string },
	queryParams?: { [key in string]: string },
	model?: string,
	modelReasoningEffort?: string,
	/**
	 *  DroidGear-only: written to config.toml top-level
	 *  `model_context_window` while this provider is active.
	 */
	modelContextWindow?: number,
	/**
	 *  DroidGear-only: written to config.toml top-level
	 *  `model_auto_compact_token_limit` while this provider is active.
	 */
	modelAutoCompactTokenLimit?: number,
	/**  Provider API key. Written to config.toml as `experimental_bearer_token`. */
	apiKey?: string,
};

/**  Session detail with messages */
export type CodexSessionDetail = CodexSessionDetail_Serialize | CodexSessionDetail_Deserialize;

/**  Session detail with messages */
export type CodexSessionDetail_Deserialize = {
	id: string,
	title: string,
	cwd: string,
	model: string,
	modelProvider: string,
	modifiedAt: number | null,
	tokenUsage: CodexTokenUsage,
	messages: CodexSessionMessage_Deserialize[],
};

/**  Session detail with messages */
export type CodexSessionDetail_Serialize = {
	id: string,
	title: string,
	cwd: string,
	model: string,
	modelProvider: string,
	modifiedAt: number | null,
	tokenUsage: CodexTokenUsage,
	messages: CodexSessionMessage_Serialize[],
};

/**  Session message */
export type CodexSessionMessage = CodexSessionMessage_Serialize | CodexSessionMessage_Deserialize;

/**  Session message */
export type CodexSessionMessage_Deserialize = {
	id: string,
	role: string,
	content: CodexContentBlock_Deserialize[],
	timestamp: string,
};

/**  Session message */
export type CodexSessionMessage_Serialize = {
	id: string,
	role: string,
	content: CodexContentBlock_Serialize[],
	timestamp: string,
};

/**  A Codex model provider aggregated from all configured profiles. */
export type CodexSessionProvider = {
	/**  Provider id as used in config.toml (`model_provider`) and session files */
	id: string,
	/**  Display name (provider config name, or the id itself) */
	name: string,
};

/**  Session summary for the list view. */
export type CodexSessionSummary = {
	/**  Session UUID */
	id: string,
	/**  Session title */
	title: string,
	/**  Working directory the session ran in (used as its "project") */
	project: string,
	/**  Model used */
	model: string,
	/**  Model provider owning this session (from session_meta) */
	modelProvider: string,
	/**  Last modified timestamp in milliseconds */
	modifiedAt: number | null,
	/**  Token usage */
	tokenUsage: CodexTokenUsage,
	/**  Full path to the session .jsonl file */
	path: string,
};

export type CodexTemporaryRunPlan = {
	program: string,
	args: string[],
	env: ([string, string])[],
	unsetEnv: string[],
	secretEnvKeys: string[],
	warnings: string[],
};

/**  Token usage statistics (from the last `event_msg/token_count` event). */
export type CodexTokenUsage = {
	inputTokens: number | null,
	outputTokens: number | null,
	cacheCreationTokens: number | null,
	cacheReadTokens: number | null,
	reasoningTokens: number | null,
	totalTokens: number | null,
};

/**  User-defined configuration paths (only stores explicitly set paths) */
export type ConfigPaths = ConfigPaths_Serialize | ConfigPaths_Deserialize;

/**  User-defined configuration paths (only stores explicitly set paths) */
export type ConfigPaths_Deserialize = {
	factory: string | null,
	opencode: string | null,
	opencodeAuth: string | null,
	codex: string | null,
	claude: string | null,
	openclaw: string | null,
	hermes: string | null,
	pi: string | null,
	omp: string | null,
};

/**  User-defined configuration paths (only stores explicitly set paths) */
export type ConfigPaths_Serialize = {
	factory?: string,
	opencode?: string,
	opencodeAuth?: string,
	codex?: string,
	claude?: string,
	openclaw?: string,
	hermes?: string,
	pi?: string,
	omp?: string,
};

export type ConnectionDiagnostics = ConnectionDiagnostics_Serialize | ConnectionDiagnostics_Deserialize;

export type ConnectionDiagnostics_Deserialize = {
	success: boolean,
	provider: string,
	modelId: string,
	latencyMs: number,
	error: string | null,
	timestamp: string,
	testMode: TestMode,
	/**  Actual model response text (inference mode only). */
	responseText: string | null,
	/**  The prompt that was sent (inference mode only). */
	promptUsed: string | null,
};

export type ConnectionDiagnostics_Serialize = {
	success: boolean,
	provider: string,
	modelId: string,
	latencyMs: number,
	error?: string,
	timestamp: string,
	testMode: TestMode,
	/**  Actual model response text (inference mode only). */
	responseText?: string,
	/**  The prompt that was sent (inference mode only). */
	promptUsed?: string,
};

export type ConnectivitySummary = {
	totalModels: number,
	availableModels: number,
	unavailableModels: number,
	avgLatencyMs: number | null,
	lastUpdated: string,
};

/**  Message content block */
export type ContentBlock = ContentBlock_Serialize | ContentBlock_Deserialize;

/**  Message content block */
export type ContentBlock_Deserialize = {
	type: string,
	text: string | null,
	thinking: string | null,
};

/**  Message content block */
export type ContentBlock_Serialize = {
	type: string,
	text?: string,
	thinking?: string,
};

/**  Custom model configuration */
export type CustomModel = CustomModel_Serialize | CustomModel_Deserialize;

/**  Custom model configuration */
export type CustomModel_Deserialize = {
	/**  Model identifier sent via API */
	model: string,
	/**  Unique identifier for the model (e.g., "custom:ModelName-0") */
	id: string | null,
	/**  Index of the model in the list */
	index: number | null,
	/**  Human-friendly name shown in model selector */
	displayName: string | null,
	/**  API endpoint base URL */
	baseUrl: string,
	/**  API key for the provider */
	apiKey: string,
	/**  Provider type */
	provider: Provider,
	/**  Maximum output tokens */
	maxOutputTokens: number | null,
	/**  Whether to disable image support for this model (default: images supported) */
	noImageSupport: boolean | null,
	/**  Additional provider-specific arguments */
	extraArgs: { [key in string]: Value } | null,
	/**  Additional HTTP headers */
	extraHeaders: { [key in string]: string } | null,
};

/**  Custom model configuration */
export type CustomModel_Serialize = {
	/**  Model identifier sent via API */
	model: string,
	/**  Unique identifier for the model (e.g., "custom:ModelName-0") */
	id?: string,
	/**  Index of the model in the list */
	index?: number,
	/**  Human-friendly name shown in model selector */
	displayName?: string,
	/**  API endpoint base URL */
	baseUrl: string,
	/**  API key for the provider */
	apiKey: string,
	/**  Provider type */
	provider: Provider,
	/**  Maximum output tokens */
	maxOutputTokens?: number,
	/**  Whether to disable image support for this model (default: images supported) */
	noImageSupport?: boolean,
	/**  Additional provider-specific arguments */
	extraArgs?: { [key in string]: Value },
	/**  Additional HTTP headers */
	extraHeaders?: { [key in string]: string },
};

export type DroidRunPreferences = DroidRunPreferences_Serialize | DroidRunPreferences_Deserialize;

export type DroidRunPreferences_Deserialize = {
	disableAutoUpdateEnv?: boolean | null,
	unsetAnthropicAuthToken?: boolean | null,
};

export type DroidRunPreferences_Serialize = {
	disableAutoUpdateEnv: boolean | null,
	unsetAnthropicAuthToken: boolean | null,
};

/**  Dsh provider compatibility flags. Unknown fields are retained. */
export type DshCompatConfig = DshCompatConfig_Serialize | DshCompatConfig_Deserialize;

/**  Dsh provider compatibility flags. Unknown fields are retained. */
export type DshCompatConfig_Deserialize = {
	supportsDeveloperRole: boolean | null,
};

/**  Dsh provider compatibility flags. Unknown fields are retained. */
export type DshCompatConfig_Serialize = {
	supportsDeveloperRole?: boolean,
};

/**  Dsh settings.yaml file status. */
export type DshConfigStatus = {
	configExists: boolean,
	configPath: string,
	credentialsExists: boolean,
	credentialsPath: string,
};

/**  Credentials read from `~/.dsh/.credentials.yaml` (version + env refs). */
export type DshCredentials = DshCredentials_Serialize | DshCredentials_Deserialize;

/**  Credentials read from `~/.dsh/.credentials.yaml` (version + env refs). */
export type DshCredentials_Deserialize = {
	version?: number,
	/**  Environment variable name → API key value. */
	refs?: { [key in string]: string },
};

/**  Credentials read from `~/.dsh/.credentials.yaml` (version + env refs). */
export type DshCredentials_Serialize = {
	version: number,
	/**  Environment variable name → API key value. */
	refs: { [key in string]: string },
};

/**  Current Dsh configuration read from `~/.dsh/settings.yaml`. */
export type DshCurrentConfig = DshCurrentConfig_Serialize | DshCurrentConfig_Deserialize;

/**  Current Dsh configuration read from `~/.dsh/settings.yaml`. */
export type DshCurrentConfig_Deserialize = {
	providers?: { [key in string]: DshProviderConfig_Deserialize },
};

/**  Current Dsh configuration read from `~/.dsh/settings.yaml`. */
export type DshCurrentConfig_Serialize = {
	providers: { [key in string]: DshProviderConfig_Serialize },
};

/**  Dsh model definition inside a provider. */
export type DshModel = DshModel_Serialize | DshModel_Deserialize;

/**  Dsh model definition inside a provider. */
export type DshModel_Deserialize = {
	id: string,
	name: string | null,
	contextWindow: number | null,
	maxTokens: number | null,
	/**  Reasoning effort mapping, e.g. `{"off": null, "high": "high"}`. */
	reasoningEfforts: { [key in string]: string | null } | null,
};

/**  Dsh model definition inside a provider. */
export type DshModel_Serialize = {
	id: string,
	name?: string,
	contextWindow?: number,
	maxTokens?: number,
	/**  Reasoning effort mapping, e.g. `{"off": null, "high": "high"}`. */
	reasoningEfforts?: { [key in string]: string | null },
};

/**  Dsh provider configuration (one entry of `llm-pi-ai.providers`). */
export type DshProviderConfig = DshProviderConfig_Serialize | DshProviderConfig_Deserialize;

/**  Dsh provider configuration (one entry of `llm-pi-ai.providers`). */
export type DshProviderConfig_Deserialize = {
	displayName: string | null,
	/**  The file uses `baseURL` (not `baseUrl`). */
	baseURL: string | null,
	apiKeyEnv: string | null,
	api: string | null,
	compat: DshCompatConfig_Deserialize | null,
	models?: DshModel_Deserialize[],
};

/**  Dsh provider configuration (one entry of `llm-pi-ai.providers`). */
export type DshProviderConfig_Serialize = {
	displayName?: string,
	/**  The file uses `baseURL` (not `baseUrl`). */
	baseURL?: string,
	apiKeyEnv?: string,
	api?: string,
	compat?: DshCompatConfig_Serialize,
	models?: DshModel_Serialize[],
};

/**  Effective path info with default indicator */
export type EffectivePath = {
	key: string,
	path: string,
	isDefault: boolean,
};

/**  All effective paths */
export type EffectivePaths = {
	factory: EffectivePath,
	opencode: EffectivePath,
	opencodeAuth: EffectivePath,
	codex: EffectivePath,
	claude: EffectivePath,
	openclaw: EffectivePath,
	hermes: EffectivePath,
	pi: EffectivePath,
	omp: EffectivePath,
};

/**  Output format */
export type ExportFormat = "json" | "yaml" | "toml";

/**  Result of an export run. */
export type ExportResult = ExportResult_Serialize | ExportResult_Deserialize;

/**  Result of an export run. */
export type ExportResult_Deserialize = {
	/**  Template name */
	template: string,
	/**  Number of channels processed */
	channelsCount: number,
	/**  Number of tokens processed */
	tokensCount: number,
	/**  Number of models exported */
	modelsCount: number,
	/**  Output file path */
	outputPath: string,
	/**  Record count (rows written) */
	recordCount: number,
	/**  Any warnings */
	warnings?: string[],
};

/**  Result of an export run. */
export type ExportResult_Serialize = {
	/**  Template name */
	template: string,
	/**  Number of channels processed */
	channelsCount: number,
	/**  Number of tokens processed */
	tokensCount: number,
	/**  Number of models exported */
	modelsCount: number,
	/**  Output file path */
	outputPath: string,
	/**  Record count (rows written) */
	recordCount: number,
	/**  Any warnings */
	warnings?: string[],
};

/**  A single export template ("the form") */
export type ExportTemplate = ExportTemplate_Serialize | ExportTemplate_Deserialize;

/**  A single export template ("the form") */
export type ExportTemplate_Deserialize = {
	/**  Template name (unique identifier) */
	name: string,
	/**  Human-readable description */
	description?: string,
	channels?: ChannelFilter_Deserialize,
	tokens?: TokenFilter_Deserialize,
	/**  Whether to fetch the model list from the API */
	fetchModels?: boolean,
	/**
	 *  Optional protocol overrides by model ID prefix (glob-like)
	 *  e.g. {"claude-*": "anthropic"}
	 */
	modelProtocolOverrides?: { [key in string]: string },
	/**
	 *  Map of source field path → output field name
	 *  e.g. {"channel.name": "channel", "model.id": "model", "token.key": "apiKey"}
	 *  Empty = all fields with original names
	 */
	fields?: { [key in string]: string },
	format: ExportFormat,
	outputStructure?: OutputStructure,
	/**  Output file path (supports ~ and {timestamp}) */
	outputPath: string,
};

/**  A single export template ("the form") */
export type ExportTemplate_Serialize = {
	/**  Template name (unique identifier) */
	name: string,
	/**  Human-readable description */
	description: string,
	channels: ChannelFilter_Serialize,
	tokens: TokenFilter_Serialize,
	/**  Whether to fetch the model list from the API */
	fetchModels: boolean,
	/**
	 *  Optional protocol overrides by model ID prefix (glob-like)
	 *  e.g. {"claude-*": "anthropic"}
	 */
	modelProtocolOverrides: { [key in string]: string },
	/**
	 *  Map of source field path → output field name
	 *  e.g. {"channel.name": "channel", "model.id": "model", "token.key": "apiKey"}
	 *  Empty = all fields with original names
	 */
	fields: { [key in string]: string },
	format: ExportFormat,
	outputStructure: OutputStructure,
	/**  Output file path (supports ~ and {timestamp}) */
	outputPath: string,
};

/**  Hermes Live 配置状态 */
export type HermesConfigStatus = {
	configExists: boolean,
	configPath: string,
};

/**  当前 Hermes Live 配置（从 `~/.hermes/config.yaml` 读取） */
export type HermesCurrentConfig = HermesCurrentConfig_Serialize | HermesCurrentConfig_Deserialize;

/**  当前 Hermes Live 配置（从 `~/.hermes/config.yaml` 读取） */
export type HermesCurrentConfig_Deserialize = {
	/**
	 *  模型配置列表：live 的 `model` 节合并 `custom_providers` 列表；
	 *  `is_default` 标记当前生效（model.default/model.provider）的条目。
	 */
	models: HermesModelConfig_Deserialize[],
	/**  推理努力程度（对应 config.yaml 中的 agent.reasoning_effort） */
	reasoningEffort: string | null,
};

/**  当前 Hermes Live 配置（从 `~/.hermes/config.yaml` 读取） */
export type HermesCurrentConfig_Serialize = {
	/**
	 *  模型配置列表：live 的 `model` 节合并 `custom_providers` 列表；
	 *  `is_default` 标记当前生效（model.default/model.provider）的条目。
	 */
	models: HermesModelConfig_Serialize[],
	/**  推理努力程度（对应 config.yaml 中的 agent.reasoning_effort） */
	reasoningEffort?: string,
};

/**
 *  单条 Hermes model 配置。
 * 
 *  apply 时：
 *  - `is_default` 的条目 → 写入 config.yaml 的 `model.default` / `model.provider`；
 *  - 解析出 `custom:<name>` 且带 base_url 的条目 → 写入 `custom_providers` 列表；
 *  - 裸 `custom`（无 name）→ base_url/api_key 直接写入 `model` 节。
 */
export type HermesModelConfig = HermesModelConfig_Serialize | HermesModelConfig_Deserialize;

/**
 *  单条 Hermes model 配置。
 * 
 *  apply 时：
 *  - `is_default` 的条目 → 写入 config.yaml 的 `model.default` / `model.provider`；
 *  - 解析出 `custom:<name>` 且带 base_url 的条目 → 写入 `custom_providers` 列表；
 *  - 裸 `custom`（无 name）→ base_url/api_key 直接写入 `model` 节。
 */
export type HermesModelConfig_Deserialize = {
	/**  配置名称（对应 `custom_providers[].name`，也是 `custom:<name>` 的引用名）。 */
	name: string | null,
	/**  模型 ID（写入 `model.default` 或 `custom_providers[].model`）。 */
	default: string | null,
	/**  显式 provider（如 openrouter / deepseek / custom）。留空时按 name/base_url 推导。 */
	provider: string | null,
	/**  OpenAI 兼容端点地址（自定义供应商使用）。 */
	baseUrl: string | null,
	/**  API 密钥（自定义供应商使用）。 */
	apiKey: string | null,
	/**  是否为默认配置（apply 时写入 `model.default` + `model.provider`）。 */
	isDefault?: boolean,
};

/**
 *  单条 Hermes model 配置。
 * 
 *  apply 时：
 *  - `is_default` 的条目 → 写入 config.yaml 的 `model.default` / `model.provider`；
 *  - 解析出 `custom:<name>` 且带 base_url 的条目 → 写入 `custom_providers` 列表；
 *  - 裸 `custom`（无 name）→ base_url/api_key 直接写入 `model` 节。
 */
export type HermesModelConfig_Serialize = {
	/**  配置名称（对应 `custom_providers[].name`，也是 `custom:<name>` 的引用名）。 */
	name?: string,
	/**  模型 ID（写入 `model.default` 或 `custom_providers[].model`）。 */
	default?: string,
	/**  显式 provider（如 openrouter / deepseek / custom）。留空时按 name/base_url 推导。 */
	provider?: string,
	/**  OpenAI 兼容端点地址（自定义供应商使用）。 */
	baseUrl?: string,
	/**  API 密钥（自定义供应商使用）。 */
	apiKey?: string,
	/**  是否为默认配置（apply 时写入 `model.default` + `model.provider`）。 */
	isDefault?: boolean,
};

/**
 *  Hermes Profile（用于在 DroidGear 内部保存并切换）。
 * 
 *  旧版 Profile 只有单条 `model` 对象；反序列化时会自动迁移为单元素的
 *  `models` 列表（并标记 is_default）。
 */
export type HermesProfile = HermesProfile_Serialize | HermesProfile_Deserialize;

/**
 *  Hermes Profile（用于在 DroidGear 内部保存并切换）。
 * 
 *  旧版 Profile 只有单条 `model` 对象；反序列化时会自动迁移为单元素的
 *  `models` 列表（并标记 is_default）。
 */
export type HermesProfile_Deserialize = {
	id: string,
	name: string,
	description: string | null,
	createdAt: string,
	updatedAt: string,
	/**  模型配置列表（至少一条应标记 is_default）。 */
	models: HermesModelConfig_Deserialize[],
	/**
	 *  推理努力程度（对应 config.yaml 中的 agent.reasoning_effort）
	 *  选项：none, minimal, low, medium, high, xhigh, max, ultra
	 */
	reasoningEffort: string | null,
};

/**
 *  Hermes Profile（用于在 DroidGear 内部保存并切换）。
 * 
 *  旧版 Profile 只有单条 `model` 对象；反序列化时会自动迁移为单元素的
 *  `models` 列表（并标记 is_default）。
 */
export type HermesProfile_Serialize = {
	id: string,
	name: string,
	description?: string,
	createdAt: string,
	updatedAt: string,
	/**  模型配置列表（至少一条应标记 is_default）。 */
	models: HermesModelConfig_Serialize[],
	/**
	 *  推理努力程度（对应 config.yaml 中的 agent.reasoning_effort）
	 *  选项：none, minimal, low, medium, high, xhigh, max, ultra
	 */
	reasoningEffort?: string,
};

/**  MCP server entry with name */
export type McpServer = McpServer_Serialize | McpServer_Deserialize;

/**  MCP server configuration */
export type McpServerConfig = McpServerConfig_Serialize | McpServerConfig_Deserialize;

/**  MCP server configuration */
export type McpServerConfig_Deserialize = {
	/**  Server type (stdio or http) */
	type: McpServerType,
	/**  Whether the server is disabled */
	disabled?: boolean,
	/**  Command to run (stdio only) */
	command: string | null,
	/**  Command arguments (stdio only) */
	args: string[] | null,
	/**  Environment variables (stdio only) */
	env: { [key in string]: string } | null,
	/**  HTTP URL (http only) */
	url: string | null,
	/**  HTTP headers (http only) */
	headers: { [key in string]: string } | null,
};

/**  MCP server configuration */
export type McpServerConfig_Serialize = {
	/**  Server type (stdio or http) */
	type: McpServerType,
	/**  Whether the server is disabled */
	disabled: boolean,
	/**  Command to run (stdio only) */
	command?: string,
	/**  Command arguments (stdio only) */
	args?: string[],
	/**  Environment variables (stdio only) */
	env?: { [key in string]: string },
	/**  HTTP URL (http only) */
	url?: string,
	/**  HTTP headers (http only) */
	headers?: { [key in string]: string },
};

/**  MCP server type */
export type McpServerType = "stdio" | "http";

/**  MCP server entry with name */
export type McpServer_Deserialize = {
	/**  Server name (unique identifier) */
	name: string,
	/**  Server configuration */
	config: McpServerConfig_Deserialize,
};

/**  MCP server entry with name */
export type McpServer_Serialize = {
	/**  Server name (unique identifier) */
	name: string,
	/**  Server configuration */
	config: McpServerConfig_Serialize,
};

/**  Mission model settings for Mission mode workers */
export type MissionModelSettings = MissionModelSettings_Serialize | MissionModelSettings_Deserialize;

/**  Mission model settings for Mission mode workers */
export type MissionModelSettings_Deserialize = {
	workerModel: string | null,
	workerReasoningEffort: string | null,
	validationWorkerModel: string | null,
	validationWorkerReasoningEffort: string | null,
};

/**  Mission model settings for Mission mode workers */
export type MissionModelSettings_Serialize = {
	workerModel?: string,
	workerReasoningEffort?: string,
	validationWorkerModel?: string,
	validationWorkerReasoningEffort?: string,
};

/**  Model info returned from API */
export type ModelInfo = {
	id: string,
	name: string | null,
};

export type ModelTestResult = ModelTestResult_Serialize | ModelTestResult_Deserialize;

export type ModelTestResult_Deserialize = {
	modelId: string,
	modelName: string,
	diagnostics: ConnectionDiagnostics_Deserialize,
	isAvailable: boolean,
};

export type ModelTestResult_Serialize = {
	modelId: string,
	modelName: string,
	diagnostics: ConnectionDiagnostics_Serialize,
	isAvailable: boolean,
};

/**  OMP agent configuration (from `~/.omp/agent/config.yml`). */
export type OmpAgentConfig = OmpAgentConfig_Serialize | OmpAgentConfig_Deserialize;

/**  OMP agent configuration (from `~/.omp/agent/config.yml`). */
export type OmpAgentConfig_Deserialize = {
	modelRoles: OmpModelRoles_Deserialize | null,
	theme: OmpTheme_Deserialize | null,
	steeringMode: string | null,
	followUpMode: string | null,
	interruptMode: string | null,
	symbolPreset: string | null,
	setupVersion: number | null,
};

/**  OMP agent configuration (from `~/.omp/agent/config.yml`). */
export type OmpAgentConfig_Serialize = {
	modelRoles?: OmpModelRoles_Serialize,
	theme?: OmpTheme_Serialize,
	steeringMode?: string,
	followUpMode?: string,
	interruptMode?: string,
	symbolPreset?: string,
	setupVersion?: number,
};

/**  A cached model entry from `models.db` (read-only). */
export type OmpCachedModel = OmpCachedModel_Serialize | OmpCachedModel_Deserialize;

/**  A cached model entry from `models.db` (read-only). */
export type OmpCachedModel_Deserialize = {
	id: string,
	name: string | null,
	api: string | null,
	provider: string | null,
	baseUrl: string | null,
	reasoning?: boolean,
	input?: string[],
	cost: OmpModelCost_Deserialize | null,
	contextWindow?: number,
	maxTokens?: number,
	thinking: OmpThinkingConfig_Deserialize | null,
};

/**  A cached model entry from `models.db` (read-only). */
export type OmpCachedModel_Serialize = {
	id: string,
	name?: string,
	api?: string,
	provider?: string,
	baseUrl?: string,
	reasoning: boolean,
	input: string[],
	cost?: OmpModelCost_Serialize,
	contextWindow: number,
	maxTokens: number,
	thinking?: OmpThinkingConfig_Serialize,
};

/**  Full OMP configuration status. */
export type OmpConfigStatus = {
	configExists: boolean,
	configPath: string,
	modelsDbExists: boolean,
	agentDbExists: boolean,
};

/**  OMP credential status for a provider. */
export type OmpCredentialStatus = {
	provider: string,
	credentialType: string,
	hasKey: boolean,
};

/**  Current OMP configuration (combined from all sources). */
export type OmpCurrentConfig = OmpCurrentConfig_Serialize | OmpCurrentConfig_Deserialize;

/**  Current OMP configuration (combined from all sources). */
export type OmpCurrentConfig_Deserialize = {
	agentConfig?: OmpAgentConfig_Deserialize,
	providerModels?: OmpProviderModels_Deserialize[],
	credentials?: OmpCredentialStatus[],
};

/**  Current OMP configuration (combined from all sources). */
export type OmpCurrentConfig_Serialize = {
	agentConfig: OmpAgentConfig_Serialize,
	providerModels: OmpProviderModels_Serialize[],
	credentials: OmpCredentialStatus[],
};

/**  OMP model cost information. */
export type OmpModelCost = OmpModelCost_Serialize | OmpModelCost_Deserialize;

/**  OMP model cost information. */
export type OmpModelCost_Deserialize = {
	input?: number | null,
	output?: number | null,
	cacheRead?: number | null,
	cacheWrite?: number | null,
};

/**  OMP model cost information. */
export type OmpModelCost_Serialize = {
	input: number | null,
	output: number | null,
	cacheRead: number | null,
	cacheWrite: number | null,
};

/**  OMP model roles — which model handles which role. */
export type OmpModelRoles = OmpModelRoles_Serialize | OmpModelRoles_Deserialize;

/**  OMP model roles — which model handles which role. */
export type OmpModelRoles_Deserialize = {
	default: string | null,
	smol: string | null,
	slow: string | null,
	plan: string | null,
	commit: string | null,
};

/**  OMP model roles — which model handles which role. */
export type OmpModelRoles_Serialize = {
	default?: string,
	smol?: string,
	slow?: string,
	plan?: string,
	commit?: string,
};

/**  OMP profile (stored in DroidGear) — snapshot of model role assignments. */
export type OmpProfile = OmpProfile_Serialize | OmpProfile_Deserialize;

/**  OMP profile (stored in DroidGear) — snapshot of model role assignments. */
export type OmpProfile_Deserialize = {
	id: string,
	name: string,
	description: string | null,
	createdAt: string,
	updatedAt: string,
	/**  Model role assignments (what gets written to config.yml on apply). */
	modelRoles?: OmpModelRoles_Deserialize,
};

/**  OMP profile (stored in DroidGear) — snapshot of model role assignments. */
export type OmpProfile_Serialize = {
	id: string,
	name: string,
	description?: string,
	createdAt: string,
	updatedAt: string,
	/**  Model role assignments (what gets written to config.yml on apply). */
	modelRoles: OmpModelRoles_Serialize,
};

/**  A provider's cached model list from `models.db`. */
export type OmpProviderModels = OmpProviderModels_Serialize | OmpProviderModels_Deserialize;

/**  A provider's cached model list from `models.db`. */
export type OmpProviderModels_Deserialize = {
	providerId: string,
	models: OmpCachedModel_Deserialize[],
};

/**  A provider's cached model list from `models.db`. */
export type OmpProviderModels_Serialize = {
	providerId: string,
	models: OmpCachedModel_Serialize[],
};

/**  Result of validating an OMP provider through HTTP connectivity testing. */
export type OmpProviderTestResult = OmpProviderTestResult_Serialize | OmpProviderTestResult_Deserialize;

/**  Result of validating an OMP provider through HTTP connectivity testing. */
export type OmpProviderTestResult_Deserialize = {
	success: boolean,
	providerId: string,
	modelId: string,
	latencyMs: number,
	responseText: string | null,
	error: string | null,
};

/**  Result of validating an OMP provider through HTTP connectivity testing. */
export type OmpProviderTestResult_Serialize = {
	success: boolean,
	providerId: string,
	modelId: string,
	latencyMs: number,
	responseText?: string,
	error?: string,
};

/**  OMP theme configuration. */
export type OmpTheme = OmpTheme_Serialize | OmpTheme_Deserialize;

/**  OMP theme configuration. */
export type OmpTheme_Deserialize = {
	dark: string | null,
	light: string | null,
};

/**  OMP theme configuration. */
export type OmpTheme_Serialize = {
	dark?: string,
	light?: string,
};

/**  OMP thinking/reasoning configuration. */
export type OmpThinkingConfig = OmpThinkingConfig_Serialize | OmpThinkingConfig_Deserialize;

/**  OMP thinking/reasoning configuration. */
export type OmpThinkingConfig_Deserialize = {
	mode: string | null,
	efforts: string[] | null,
};

/**  OMP thinking/reasoning configuration. */
export type OmpThinkingConfig_Serialize = {
	mode?: string,
	efforts?: string[],
};

/**  OpenClaw config status */
export type OpenClawConfigStatus = {
	configExists: boolean,
	configPath: string,
};

/**  Current OpenClaw configuration (from config files) */
export type OpenClawCurrentConfig = OpenClawCurrentConfig_Serialize | OpenClawCurrentConfig_Deserialize;

/**  Current OpenClaw configuration (from config files) */
export type OpenClawCurrentConfig_Deserialize = {
	defaultModel: string | null,
	providers?: { [key in string]: OpenClawProviderConfig_Deserialize },
};

/**  Current OpenClaw configuration (from config files) */
export type OpenClawCurrentConfig_Serialize = {
	defaultModel?: string,
	providers: { [key in string]: OpenClawProviderConfig_Serialize },
};

/**  OpenClaw Model definition */
export type OpenClawModel = OpenClawModel_Serialize | OpenClawModel_Deserialize;

/**  OpenClaw Model definition */
export type OpenClawModel_Deserialize = {
	id: string,
	name: string | null,
	reasoning?: boolean,
	input?: string[],
	contextWindow: number | null,
	maxTokens: number | null,
};

/**  OpenClaw Model definition */
export type OpenClawModel_Serialize = {
	id: string,
	name?: string,
	reasoning: boolean,
	input: string[],
	contextWindow?: number,
	maxTokens?: number,
};

/**  OpenClaw Profile (stored in DroidGear) */
export type OpenClawProfile = OpenClawProfile_Serialize | OpenClawProfile_Deserialize;

/**  OpenClaw Profile (stored in DroidGear) */
export type OpenClawProfile_Deserialize = {
	id: string,
	name: string,
	description: string | null,
	createdAt: string,
	updatedAt: string,
	defaultModel: string | null,
	failoverModels: string[] | null,
	providers?: { [key in string]: OpenClawProviderConfig_Deserialize },
	blockStreamingConfig: BlockStreamingConfig_Deserialize | null,
};

/**  OpenClaw Profile (stored in DroidGear) */
export type OpenClawProfile_Serialize = {
	id: string,
	name: string,
	description?: string,
	createdAt: string,
	updatedAt: string,
	defaultModel?: string,
	failoverModels?: string[],
	providers: { [key in string]: OpenClawProviderConfig_Serialize },
	blockStreamingConfig?: BlockStreamingConfig_Serialize,
};

/**  OpenClaw Provider configuration */
export type OpenClawProviderConfig = OpenClawProviderConfig_Serialize | OpenClawProviderConfig_Deserialize;

/**  OpenClaw Provider configuration */
export type OpenClawProviderConfig_Deserialize = {
	baseUrl: string | null,
	apiKey: string | null,
	api: string | null,
	models?: OpenClawModel_Deserialize[],
};

/**  OpenClaw Provider configuration */
export type OpenClawProviderConfig_Serialize = {
	baseUrl?: string,
	apiKey?: string,
	api?: string,
	models: OpenClawModel_Serialize[],
};

/**  OpenClaw SubAgent definition */
export type OpenClawSubAgent = OpenClawSubAgent_Serialize | OpenClawSubAgent_Deserialize;

/**  OpenClaw SubAgent identity */
export type OpenClawSubAgentIdentity = OpenClawSubAgentIdentity_Serialize | OpenClawSubAgentIdentity_Deserialize;

/**  OpenClaw SubAgent identity */
export type OpenClawSubAgentIdentity_Deserialize = {
	emoji: string | null,
	name: string | null,
};

/**  OpenClaw SubAgent identity */
export type OpenClawSubAgentIdentity_Serialize = {
	emoji?: string,
	name?: string,
};

/**  OpenClaw SubAgent model config */
export type OpenClawSubAgentModel = OpenClawSubAgentModel_Serialize | OpenClawSubAgentModel_Deserialize;

/**  OpenClaw SubAgent model config */
export type OpenClawSubAgentModel_Deserialize = {
	primary: string | null,
	fallbacks: string[] | null,
};

/**  OpenClaw SubAgent model config */
export type OpenClawSubAgentModel_Serialize = {
	primary?: string,
	fallbacks?: string[],
};

/**  OpenClaw SubAgent subagents config (for main agent's allowAgents) */
export type OpenClawSubAgentSubagentsConfig = OpenClawSubAgentSubagentsConfig_Serialize | OpenClawSubAgentSubagentsConfig_Deserialize;

/**  OpenClaw SubAgent subagents config (for main agent's allowAgents) */
export type OpenClawSubAgentSubagentsConfig_Deserialize = {
	allowAgents: string[] | null,
	maxConcurrent: number | null,
};

/**  OpenClaw SubAgent subagents config (for main agent's allowAgents) */
export type OpenClawSubAgentSubagentsConfig_Serialize = {
	allowAgents?: string[],
	maxConcurrent?: number,
};

/**  OpenClaw SubAgent tools config */
export type OpenClawSubAgentTools = OpenClawSubAgentTools_Serialize | OpenClawSubAgentTools_Deserialize;

/**  OpenClaw SubAgent tools config */
export type OpenClawSubAgentTools_Deserialize = {
	profile: string | null,
};

/**  OpenClaw SubAgent tools config */
export type OpenClawSubAgentTools_Serialize = {
	profile?: string,
};

/**  OpenClaw SubAgent definition */
export type OpenClawSubAgent_Deserialize = {
	id: string,
	name: string | null,
	identity: OpenClawSubAgentIdentity_Deserialize | null,
	model: OpenClawSubAgentModel_Deserialize | null,
	tools: OpenClawSubAgentTools_Deserialize | null,
	workspace: string | null,
	subagents: OpenClawSubAgentSubagentsConfig_Deserialize | null,
};

/**  OpenClaw SubAgent definition */
export type OpenClawSubAgent_Serialize = {
	id: string,
	name?: string,
	identity?: OpenClawSubAgentIdentity_Serialize,
	model?: OpenClawSubAgentModel_Serialize,
	tools?: OpenClawSubAgentTools_Serialize,
	workspace?: string,
	subagents?: OpenClawSubAgentSubagentsConfig_Serialize,
};

/**  Configuration status */
export type OpenCodeConfigStatus = {
	configExists: boolean,
	authExists: boolean,
	configPath: string,
	authPath: string,
};

/**  Current OpenCode configuration (providers and auth from config files) */
export type OpenCodeCurrentConfig = OpenCodeCurrentConfig_Serialize | OpenCodeCurrentConfig_Deserialize;

/**  Current OpenCode configuration (providers and auth from config files) */
export type OpenCodeCurrentConfig_Deserialize = {
	providers: { [key in string]: OpenCodeProviderConfig_Deserialize },
	auth: { [key in string]: Value },
};

/**  Current OpenCode configuration (providers and auth from config files) */
export type OpenCodeCurrentConfig_Serialize = {
	providers: { [key in string]: OpenCodeProviderConfig_Serialize },
	auth: { [key in string]: Value },
};

/**  OpenCode Model configuration */
export type OpenCodeModelConfig = OpenCodeModelConfig_Serialize | OpenCodeModelConfig_Deserialize;

/**  OpenCode Model configuration */
export type OpenCodeModelConfig_Deserialize = {
	name: string | null,
	limit: OpenCodeModelLimit_Deserialize | null,
};

/**  OpenCode Model configuration */
export type OpenCodeModelConfig_Serialize = {
	name?: string,
	limit?: OpenCodeModelLimit_Serialize,
};

/**  OpenCode Model limit configuration */
export type OpenCodeModelLimit = OpenCodeModelLimit_Serialize | OpenCodeModelLimit_Deserialize;

/**  OpenCode Model limit configuration */
export type OpenCodeModelLimit_Deserialize = {
	context: number | null,
	output: number | null,
};

/**  OpenCode Model limit configuration */
export type OpenCodeModelLimit_Serialize = {
	context?: number,
	output?: number,
};

/**  OpenCode Profile */
export type OpenCodeProfile = OpenCodeProfile_Serialize | OpenCodeProfile_Deserialize;

/**  OpenCode Profile */
export type OpenCodeProfile_Deserialize = {
	id: string,
	name: string,
	description: string | null,
	createdAt: string,
	updatedAt: string,
	providers: { [key in string]: OpenCodeProviderConfig_Deserialize },
	auth: { [key in string]: Value },
};

/**  OpenCode Profile */
export type OpenCodeProfile_Serialize = {
	id: string,
	name: string,
	description?: string,
	createdAt: string,
	updatedAt: string,
	providers: { [key in string]: OpenCodeProviderConfig_Serialize },
	auth: { [key in string]: Value },
};

/**  OpenCode Provider configuration */
export type OpenCodeProviderConfig = OpenCodeProviderConfig_Serialize | OpenCodeProviderConfig_Deserialize;

/**  OpenCode Provider configuration */
export type OpenCodeProviderConfig_Deserialize = {
	npm: string | null,
	name: string | null,
	options: OpenCodeProviderOptions_Deserialize | null,
	models: { [key in string]: OpenCodeModelConfig_Deserialize } | null,
};

/**  OpenCode Provider configuration */
export type OpenCodeProviderConfig_Serialize = {
	npm?: string,
	name?: string,
	options?: OpenCodeProviderOptions_Serialize,
	models?: { [key in string]: OpenCodeModelConfig_Serialize },
};

/**  OpenCode Provider options */
export type OpenCodeProviderOptions = OpenCodeProviderOptions_Serialize | OpenCodeProviderOptions_Deserialize;

/**  OpenCode Provider options */
export type OpenCodeProviderOptions_Deserialize = {
	baseURL: string | null,
	apiKey: string | null,
	timeout: number | null,
	headers: { [key in string]: string } | null,
};

/**  OpenCode Provider options */
export type OpenCodeProviderOptions_Serialize = {
	baseURL?: string,
	apiKey?: string,
	timeout?: number,
	headers?: { [key in string]: string },
};

/**  Output structure */
export type OutputStructure = "flat" | 
/**  Nested: channels → tokens → models */
"nested";

/**  Pi compatibility configuration. Unknown fields are retained for newer pi versions. */
export type PiCompatConfig = PiCompatConfig_Serialize | PiCompatConfig_Deserialize;

/**  Pi compatibility configuration. Unknown fields are retained for newer pi versions. */
export type PiCompatConfig_Deserialize = {
	supportsStore: boolean | null,
	supportsDeveloperRole: boolean | null,
	supportsReasoningEffort: boolean | null,
	reasoningEffortMap: { [key in string]: Value } | null,
	supportsUsageInStreaming: boolean | null,
	maxTokensField: string | null,
	requiresToolResultName: boolean | null,
	requiresAssistantAfterToolResult: boolean | null,
	requiresThinkingAsText: boolean | null,
	requiresReasoningContentOnAssistantMessages: boolean | null,
	thinkingFormat: string | null,
	cacheControlFormat: string | null,
	supportsStrictMode: boolean | null,
	supportsLongCacheRetention: boolean | null,
	supportsEagerToolInputStreaming: boolean | null,
	openRouterRouting: Value | null,
	vercelGatewayRouting: Value | null,
};

/**  Pi compatibility configuration. Unknown fields are retained for newer pi versions. */
export type PiCompatConfig_Serialize = {
	supportsStore?: boolean,
	supportsDeveloperRole?: boolean,
	supportsReasoningEffort?: boolean,
	reasoningEffortMap?: { [key in string]: Value },
	supportsUsageInStreaming?: boolean,
	maxTokensField?: string,
	requiresToolResultName?: boolean,
	requiresAssistantAfterToolResult?: boolean,
	requiresThinkingAsText?: boolean,
	requiresReasoningContentOnAssistantMessages?: boolean,
	thinkingFormat?: string,
	cacheControlFormat?: string,
	supportsStrictMode?: boolean,
	supportsLongCacheRetention?: boolean,
	supportsEagerToolInputStreaming?: boolean,
	openRouterRouting?: Value,
	vercelGatewayRouting?: Value,
};

/**  Pi config status */
export type PiConfigStatus = {
	configExists: boolean,
	configPath: string,
};

/**  Current Pi configuration (from `~/.pi/agent/models.json`) */
export type PiCurrentConfig = PiCurrentConfig_Serialize | PiCurrentConfig_Deserialize;

/**  Current Pi configuration (from `~/.pi/agent/models.json`) */
export type PiCurrentConfig_Deserialize = {
	providers?: { [key in string]: PiProviderConfig_Deserialize },
};

/**  Current Pi configuration (from `~/.pi/agent/models.json`) */
export type PiCurrentConfig_Serialize = {
	providers: { [key in string]: PiProviderConfig_Serialize },
};

/**  Pi model definition */
export type PiModel = PiModel_Serialize | PiModel_Deserialize;

/**  Pi model cost configuration */
export type PiModelCost = PiModelCost_Serialize | PiModelCost_Deserialize;

/**  Pi model cost tier. Rates are in dollars per million tokens. */
export type PiModelCostTier = {
	inputTokensAbove: number,
	input: number | null,
	output: number | null,
	cacheRead: number | null,
	cacheWrite: number | null,
};

/**  Pi model cost configuration */
export type PiModelCost_Deserialize = {
	input?: number | null,
	output?: number | null,
	cacheRead?: number | null,
	cacheWrite?: number | null,
	tiers: PiModelCostTier[] | null,
};

/**  Pi model cost configuration */
export type PiModelCost_Serialize = {
	input: number | null,
	output: number | null,
	cacheRead: number | null,
	cacheWrite: number | null,
	tiers?: PiModelCostTier[],
};

/**  Pi model override (subset of PiModel fields for overriding built-in models) */
export type PiModelOverride = PiModelOverride_Serialize | PiModelOverride_Deserialize;

/**  Pi model override (subset of PiModel fields for overriding built-in models) */
export type PiModelOverride_Deserialize = {
	name: string | null,
	api: string | null,
	reasoning: boolean | null,
	thinkingLevelMap: { [key in string]: string | null } | null,
	input: string[] | null,
	contextWindow: number | null,
	maxTokens: number | null,
	cost: PiModelCost_Deserialize | null,
	headers: { [key in string]: string } | null,
	compat: PiCompatConfig_Deserialize | null,
};

/**  Pi model override (subset of PiModel fields for overriding built-in models) */
export type PiModelOverride_Serialize = {
	name?: string,
	api?: string,
	reasoning?: boolean,
	thinkingLevelMap?: { [key in string]: string | null },
	input?: string[],
	contextWindow?: number,
	maxTokens?: number,
	cost?: PiModelCost_Serialize,
	headers?: { [key in string]: string },
	compat?: PiCompatConfig_Serialize,
};

/**  Pi model definition */
export type PiModel_Deserialize = {
	id: string,
	name: string | null,
	api: string | null,
	reasoning?: boolean,
	thinkingLevelMap: { [key in string]: string | null } | null,
	input?: string[],
	contextWindow?: number,
	maxTokens?: number,
	cost: PiModelCost_Deserialize | null,
	headers: { [key in string]: string } | null,
	compat: PiCompatConfig_Deserialize | null,
};

/**  Pi model definition */
export type PiModel_Serialize = {
	id: string,
	name?: string,
	api?: string,
	reasoning: boolean,
	thinkingLevelMap?: { [key in string]: string | null },
	input: string[],
	contextWindow: number,
	maxTokens: number,
	cost?: PiModelCost_Serialize,
	headers?: { [key in string]: string },
	compat?: PiCompatConfig_Serialize,
};

/**  Pi profile (stored in DroidGear) */
export type PiProfile = PiProfile_Serialize | PiProfile_Deserialize;

/**  Pi profile (stored in DroidGear) */
export type PiProfile_Deserialize = {
	id: string,
	name: string,
	description: string | null,
	createdAt: string,
	updatedAt: string,
	providers?: { [key in string]: PiProviderConfig_Deserialize },
};

/**  Pi profile (stored in DroidGear) */
export type PiProfile_Serialize = {
	id: string,
	name: string,
	description?: string,
	createdAt: string,
	updatedAt: string,
	providers: { [key in string]: PiProviderConfig_Serialize },
};

/**  Pi provider configuration */
export type PiProviderConfig = PiProviderConfig_Serialize | PiProviderConfig_Deserialize;

/**  Pi provider configuration */
export type PiProviderConfig_Deserialize = {
	baseUrl: string | null,
	api: string | null,
	apiKey: string | null,
	oauth: string | null,
	headers: { [key in string]: string } | null,
	authHeader: boolean | null,
	models?: PiModel_Deserialize[],
	modelOverrides: { [key in string]: PiModelOverride_Deserialize } | null,
	compat: PiCompatConfig_Deserialize | null,
};

/**  Pi provider configuration */
export type PiProviderConfig_Serialize = {
	baseUrl?: string,
	api?: string,
	apiKey?: string,
	oauth?: string,
	headers?: { [key in string]: string },
	authHeader?: boolean,
	models?: PiModel_Serialize[],
	modelOverrides?: { [key in string]: PiModelOverride_Serialize },
	compat?: PiCompatConfig_Serialize,
};

/**  Result of validating a provider through Pi's own CLI runtime. */
export type PiProviderTestResult = PiProviderTestResult_Serialize | PiProviderTestResult_Deserialize;

/**  Result of validating a provider through Pi's own CLI runtime. */
export type PiProviderTestResult_Deserialize = {
	success: boolean,
	providerId: string,
	modelId: string,
	latencyMs: number,
	responseText: string | null,
	error: string | null,
};

/**  Result of validating a provider through Pi's own CLI runtime. */
export type PiProviderTestResult_Serialize = {
	success: boolean,
	providerId: string,
	modelId: string,
	latencyMs: number,
	responseText?: string,
	error?: string,
};

export type PiSessionDetail = PiSessionDetail_Serialize | PiSessionDetail_Deserialize;

export type PiSessionDetail_Deserialize = {
	summary: PiSessionSummary,
	messages: PiSessionMessage_Deserialize[],
};

export type PiSessionDetail_Serialize = {
	summary: PiSessionSummary,
	messages: PiSessionMessage_Serialize[],
};

export type PiSessionMessage = PiSessionMessage_Serialize | PiSessionMessage_Deserialize;

export type PiSessionMessage_Deserialize = {
	id: string,
	role: string,
	content: ContentBlock_Deserialize[],
	timestamp: string,
	isActiveBranch: boolean,
};

export type PiSessionMessage_Serialize = {
	id: string,
	role: string,
	content: ContentBlock_Serialize[],
	timestamp: string,
	isActiveBranch: boolean,
};

export type PiSessionSummary = {
	id: string,
	title: string,
	project: string,
	model: string,
	modelProvider: string,
	modifiedAt: number | null,
	messageCount: number,
	tokenUsage: PiTokenUsage,
	path: string,
};

export type PiTokenUsage = {
	inputTokens: number | null,
	outputTokens: number | null,
	cacheReadTokens: number | null,
	cacheCreationTokens: number | null,
	reasoningTokens: number | null,
	totalTokens: number | null,
	cost: number | null,
};

export type PortableUpdateInfo = {
	version: string,
	body: string | null,
	pubDate: string | null,
	url: string,
	signature: string,
	sha256: string,
	releaseUrl: string,
};

/**  Provider types supported by Factory BYOK */
export type Provider = "anthropic" | "openai" | "generic-chat-completion-api";

/**  Provider template for quick setup */
export type ProviderTemplate = ProviderTemplate_Serialize | ProviderTemplate_Deserialize;

/**  Provider template for quick setup */
export type ProviderTemplate_Deserialize = {
	id: string,
	name: string,
	npm: string | null,
	defaultBaseUrl: string | null,
	requiresApiKey: boolean,
};

/**  Provider template for quick setup */
export type ProviderTemplate_Serialize = {
	id: string,
	name: string,
	npm?: string,
	defaultBaseUrl?: string,
	requiresApiKey: boolean,
};

/**  Error types for recovery operations (typed for frontend matching) */
export type RecoveryError = 
/**  File does not exist (expected case, not a failure) */
{ type: "FileNotFound" } | 
/**  Filename validation failed */
{ type: "ValidationError"; message: string } | 
/**  Data exceeds size limit */
{ type: "DataTooLarge"; max_bytes: number } | 
/**  File system read/write error */
{ type: "IoError"; message: string } | 
/**  JSON serialization/deserialization error */
{ type: "ParseError"; message: string };

/**  Session default settings for mixed models configuration */
export type SessionDefaultSettings = SessionDefaultSettings_Serialize | SessionDefaultSettings_Deserialize;

/**  Session default settings for mixed models configuration */
export type SessionDefaultSettings_Deserialize = {
	model: string | null,
	reasoningEffort: string | null,
	specModeModel: string | null,
	specModeReasoningEffort: string | null,
	autonomyMode: string | null,
	autonomyLevel: string | null,
	interactionMode: string | null,
};

/**  Session default settings for mixed models configuration */
export type SessionDefaultSettings_Serialize = {
	model?: string,
	reasoningEffort?: string,
	specModeModel?: string,
	specModeReasoningEffort?: string,
	autonomyMode?: string,
	autonomyLevel?: string,
	interactionMode?: string,
};

/**  Session detail with messages */
export type SessionDetail = SessionDetail_Serialize | SessionDetail_Deserialize;

/**  Session detail with messages */
export type SessionDetail_Deserialize = {
	id: string,
	title: string,
	project: string,
	model: string,
	cwd: string,
	modifiedAt: number | null,
	tokenUsage: TokenUsage,
	messages: SessionMessage_Deserialize[],
};

/**  Session detail with messages */
export type SessionDetail_Serialize = {
	id: string,
	title: string,
	project: string,
	model: string,
	cwd: string,
	modifiedAt: number | null,
	tokenUsage: TokenUsage,
	messages: SessionMessage_Serialize[],
};

/**  Session message */
export type SessionMessage = SessionMessage_Serialize | SessionMessage_Deserialize;

/**  Session message */
export type SessionMessage_Deserialize = {
	id: string,
	role: string,
	content: ContentBlock_Deserialize[],
	timestamp: string,
};

/**  Session message */
export type SessionMessage_Serialize = {
	id: string,
	role: string,
	content: ContentBlock_Serialize[],
	timestamp: string,
};

/**  Session project (directory containing sessions) */
export type SessionProject = {
	/**  Directory name (e.g., "-Users-sunshow-GIT-sunshow-quickcast-api") */
	name: string,
	/**  Full path to the directory */
	path: string,
	/**  Number of sessions in this project */
	sessionCount: number,
	/**  Last modified timestamp in milliseconds */
	modifiedAt: number | null,
};

/**  Session summary for list view */
export type SessionSummary = {
	/**  Session UUID */
	id: string,
	/**  Session title */
	title: string,
	/**  Project directory name */
	project: string,
	/**  Model used */
	model: string,
	/**  Last modified timestamp in milliseconds */
	modifiedAt: number | null,
	/**  Token usage */
	tokenUsage: TokenUsage,
	/**  Full path to the session files (without extension) */
	path: string,
};

/**  Information about a single settings file */
export type SettingsFileInfo = {
	/**  Display name ("Global" or the filename without extension) */
	name: string,
	/**  Full path to the settings file */
	path: string,
	/**  Whether this is the global `~/.factory/settings.json` */
	isGlobal: boolean,
	/**  Whether this file is currently active for editing */
	isActive: boolean,
	/**  Whether the file exists on disk */
	exists: boolean,
};

/**  Spec file metadata */
export type SpecFile = {
	/**  File name (e.g., "2025-12-18-ui.md") */
	name: string,
	/**  Full path to the file */
	path: string,
	/**  File content */
	content: string,
	/**  Last modified timestamp in milliseconds */
	modifiedAt: number | null,
};

/**  Telegram channel configuration */
export type TelegramChannelConfig = TelegramChannelConfig_Serialize | TelegramChannelConfig_Deserialize;

/**  Telegram channel configuration */
export type TelegramChannelConfig_Deserialize = {
	blockStreaming: boolean | null,
	chunkMode: string | null,
};

/**  Telegram channel configuration */
export type TelegramChannelConfig_Serialize = {
	blockStreaming?: boolean,
	chunkMode?: string,
};

/**  Test mode: ping (HTTP probe) or inference (real model call). */
export type TestMode = "ping" | "inference";

/**  Token filter conditions */
export type TokenFilter = TokenFilter_Serialize | TokenFilter_Deserialize;

/**  Token filter conditions */
export type TokenFilter_Deserialize = {
	/**  Token status (1=enabled, empty = all) */
	status: number | null,
	/**  Only include tokens matching these platforms (empty = all) */
	platforms?: string[],
};

/**  Token filter conditions */
export type TokenFilter_Serialize = {
	/**  Token status (1=enabled, empty = all) */
	status: number | null,
	/**  Only include tokens matching these platforms (empty = all) */
	platforms: string[],
};

/**  Token usage statistics */
export type TokenUsage = {
	inputTokens: number | null,
	outputTokens: number | null,
	cacheCreationTokens: number | null,
	cacheReadTokens: number | null,
	thinkingTokens: number | null,
};

/**  A folder trusted by Factory Droid. */
export type TrustedFolder = {
	path: string,
	trustedAt: string,
};

export type UpdateChannel = "managed" | "portable";

export type Value = "Null" | ({ Bool: boolean }) & { Array?: never; Number?: never; Object?: never; String?: never } | ({ Number: number }) & { Array?: never; Bool?: never; Object?: never; String?: never } | ({ String: string }) & { Array?: never; Bool?: never; Number?: never; Object?: never } | ({ Array: Value[] }) & { Bool?: never; Number?: never; Object?: never; String?: never } | ({ Object: { [key in string]: Value } }) & { Array?: never; Bool?: never; Number?: never; String?: never };

/**  WSL distribution info */
export type WslDistro = {
	name: string,
	isDefault: boolean,
	version: number,
	state: string,
};

/**  WSL information including distributions and current user */
export type WslInfo = {
	available: boolean,
	distros: WslDistro[],
};

/* Tauri Specta runtime */
async function typedError<T, E>(result: Promise<T>): Promise<{ status: "ok"; data: T } | { status: "error"; error: E }> {
    try {
        return { status: "ok", data: await result };
    } catch (e) {
        if (e instanceof Error) throw e;
        return { status: "error", error: e as any };
    }
}

/** JSON-compatible value type */
export type JsonValue =
  | string
  | number
  | boolean
  | null
  | JsonValue[]
  | { [key: string]: JsonValue }

/** Rust-style Result type */
export type Result<T, E> = { status: "ok"; data: T } | { status: "error"; error: E }

